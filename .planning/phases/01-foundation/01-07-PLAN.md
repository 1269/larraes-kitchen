---
phase: 01-foundation
plan: 07
type: execute
wave: 7
depends_on: ["01-foundation-06"]
files_modified:
  - .vercel/project.json
  - .github/CODEOWNERS
autonomous: false
requirements:
  - FND-06
  - FND-07
  - FND-08
must_haves:
  truths:
    - "Vercel project is linked via `vercel link` — `.vercel/project.json` committed"
    - "All 8 env vars from `.env.example` are registered in Vercel Preview AND Vercel Production with value `__placeholder__`"
    - "Opening a PR auto-creates a Vercel Preview URL posted as a comment (GitHub ↔ Vercel integration confirmed)"
    - "`main` branch is protected: required status checks (typecheck, biome-check, content-sync, image-budget, smoke, pr-title), CODEOWNERS review required, force-push and deletion denied"
    - "Direct `git push origin main` from a local branch is rejected with branch-protection error"
    - "CODEOWNERS has the real maintainer GitHub username (placeholder replaced)"
  artifacts:
    - path: ".vercel/project.json"
      provides: "Vercel project link — points this repo at the Vercel project"
    - path: ".github/CODEOWNERS"
      provides: "Updated with real maintainer username"
  key_links:
    - from: ".github/CODEOWNERS"
      to: "GitHub branch protection"
      via: "require_code_owner_reviews: true enforces this file"
      pattern: "@[a-zA-Z0-9-]+"
    - from: ".env.example"
      to: "Vercel env var registry"
      via: "every line in .env.example is registered in vercel env"
      pattern: "__placeholder__"
---

<objective>
Activate the external integrations the Phase 1 pipeline depends on: link the repo to Vercel (D-13), register all 8 env vars in both Vercel environments (D-15 / FND-08 / Pitfall 6), confirm the GitHub↔Vercel integration creates preview URLs on PRs (FND-07), and run `scripts/setup-branch-protection.sh` to lock `main` (D-11 / FND-06) after the first green CI has created the check contexts.

Purpose: Wave 1-6 produced the config + code. Wave 7 turns it on. After this wave, every future PR flows through the validated pipeline: CI required checks + CODEOWNERS review + Vercel preview.

**Context consideration:** This wave is inherently manual-heavy because it interacts with three external services (Vercel, GitHub, the maintainer's keyboard). Tasks are structured as checkpoints that pause for the human before each external action.

Output: `.vercel/project.json` committed, Vercel env registered in both environments, `main` branch protected, CODEOWNERS has real username, PR → preview URL flow verified.
</objective>

<execution_context>
@/Users/jashia/Documents/1_Projects/larraes-kitchen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jashia/Documents/1_Projects/larraes-kitchen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-06-SUMMARY.md
@.env.example
@.github/workflows/ci.yml
@scripts/setup-branch-protection.sh
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 7.1: Gather external service facts from the maintainer</name>
  <files>— (human verification / human action; no files modified directly by this task)</files>
  <what-built>
    Planning so far has placeholders for three facts Claude cannot discover:
      - GitHub maintainer username (for CODEOWNERS)
      - Vercel account/team and whether an existing Vercel project should be linked vs. a new one created
      - GitHub repo full name (owner/repo) — required for `gh api` branch-protection call
  </what-built>
  <how-to-verify>
    Collect and paste into the chat:
      1. **Maintainer GitHub username** (e.g. `jashia`). This replaces `@MAINTAINER-USERNAME` in `.github/CODEOWNERS`.
      2. **Vercel context:**
         - Are you logged in as the correct Vercel user? (`vercel whoami`)
         - Does a Vercel project already exist for Larrae's Kitchen that we should link to (YES = use existing; NO = create new during `vercel link`)?
      3. **GitHub repo full name** — run `gh repo view --json nameWithOwner --jq '.nameWithOwner'` and paste the output (should look like `username/larraes-kitchen`).
      4. Confirm you have admin rights on the GitHub repo (required for branch protection API).
      5. Confirm `gh auth login` has been completed (`gh auth status` → Logged in).
      6. Confirm Vercel CLI is installed — if not, run `pnpm add -g vercel`.

    If any of these are not yet true, resolve before proceeding. The subsequent tasks will fail without them.
  </how-to-verify>
  <action>Human-driven step. Follow the instructions in `<how-to-verify>` above and use `<resume-signal>` to resume the executor.</action>
  <verify>Human approval via `<resume-signal>` response matching expected acceptance criteria.</verify>
  <done>Human has completed `<how-to-verify>` steps and replied with the `<resume-signal>` expected format.</done>
  <resume-signal>Paste:
  (a) GitHub username,
  (b) new-vs-existing Vercel project choice,
  (c) GitHub repo full name (owner/repo),
  and confirm gh auth + Vercel CLI are ready.</resume-signal>
</task>

<task type="auto">
  <name>Task 7.2: Update CODEOWNERS with real username</name>
  <files>.github/CODEOWNERS</files>
  <read_first>
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.github/CODEOWNERS (CURRENT — has `@MAINTAINER-USERNAME` placeholder)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-CONTEXT.md line 43 (D-11)
  </read_first>
  <action>
Replace the `@MAINTAINER-USERNAME` placeholder in `.github/CODEOWNERS` with the real GitHub username from Task 7.1. Final file should look like:
```
# Single-owner v1 (per CONTEXT D-11). Refine when the team grows.

*   @<real-github-username-here>
```

Use `sed` with the username supplied in Task 7.1:
```bash
# Replace MAINTAINER-USERNAME with the actual value from Task 7.1, e.g. "jashia":
GH_USER="<username-from-task-7.1>"
sed -i.bak "s/@MAINTAINER-USERNAME/@$GH_USER/" .github/CODEOWNERS
rm -f .github/CODEOWNERS.bak
```

**Do NOT:**
- Add additional owners yet (deferred per CONTEXT "Deferred Ideas" — `Additional CODEOWNERS granularity` is v2+)
- Leave `@MAINTAINER-USERNAME` in the file
  </action>
  <verify>
    <automated>cd /Users/jashia/Documents/1_Projects/larraes-kitchen && ! grep -q 'MAINTAINER-USERNAME' .github/CODEOWNERS && grep -qE '^\*[[:space:]]+@[a-zA-Z0-9-]+$' .github/CODEOWNERS</automated>
  </verify>
  <acceptance_criteria>
    - `.github/CODEOWNERS` no longer contains the string `MAINTAINER-USERNAME`
    - `.github/CODEOWNERS` contains a line matching `^\*\s+@[a-zA-Z0-9-]+$` (glob + real username)
  </acceptance_criteria>
  <done>CODEOWNERS has the real maintainer username; ready for branch-protection enforcement.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 7.3: Run `vercel link` to link the repo to a Vercel project</name>
  <files>— (human verification / human action; no files modified directly by this task)</files>
  <what-built>
    `vercel link` is inherently interactive (project selection UI) and MUST run under the maintainer's Vercel auth. Claude cannot automate this.
  </what-built>
  <how-to-verify>
    From repo root, run:
    ```bash
    cd /Users/jashia/Documents/1_Projects/larraes-kitchen
    vercel link
    ```
    Answer the CLI prompts:
      - Scope: the correct Vercel team/user
      - Link to existing project? yes/no per Task 7.1 answer (b)
      - If new: project name `larraes-kitchen`
      - Directory: `.` (current)

    After completion, confirm `.vercel/project.json` exists:
    ```bash
    cat .vercel/project.json
    ```
    Expected contents: `{ "projectId": "prj_...", "orgId": "team_..." }`.

    Add `.vercel/project.json` to git (it should be tracked even though `.vercel/` is in `.gitignore` — git will skip the rest of the directory):
    ```bash
    git add -f .vercel/project.json
    ```
    Confirm `.env.local` is NOT added (it may have been pulled by `vercel env pull`; it stays gitignored).

    **Then pull any existing env state (most likely empty)**:
    ```bash
    vercel env pull .env.local
    ```
    `.env.local` stays gitignored (was added to `.gitignore` in Wave 1).
  </how-to-verify>
  <action>Human-driven step. Follow the instructions in `<how-to-verify>` above and use `<resume-signal>` to resume the executor.</action>
  <verify>Human approval via `<resume-signal>` response matching expected acceptance criteria.</verify>
  <done>Human has completed `<how-to-verify>` steps and replied with the `<resume-signal>` expected format.</done>
  <resume-signal>Paste the output of `cat .vercel/project.json` to confirm link succeeded. Do NOT paste the `projectId` / `orgId` values to public channels if sensitive — confirm privately.</resume-signal>
</task>

<task type="auto">
  <name>Task 7.4: Register all 8 env vars in Vercel Preview + Production with placeholders</name>
  <files>— (no repo files modified; side effect is Vercel env registry)</files>
  <read_first>
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.env.example (CURRENT — canonical list of 8 env var names)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-RESEARCH.md lines 904-921 (Vercel setup recipe)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-RESEARCH.md lines 420-424 (Pitfall 6 — Preview env vars missing placeholders)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-CONTEXT.md line 49 (D-15)
  </read_first>
  <action>
Register every env var name in `.env.example` in BOTH Vercel environments with placeholder value. Per Pitfall 6, registering even without real values prevents deploy-time panic on missing names.

Run from repo root:
```bash
cd /Users/jashia/Documents/1_Projects/larraes-kitchen

# Extract var names from .env.example
VARS=$(grep -oE '^[A-Z][A-Z_]+' .env.example | sort -u)
echo "Registering: $VARS"

for VAR in $VARS; do
  # Preview
  if echo "__placeholder__" | vercel env add "$VAR" preview 2>&1 | grep -qi 'already exists'; then
    echo "  $VAR preview: already exists (skip)"
  else
    echo "  $VAR preview: registered"
  fi
  # Production
  if echo "__placeholder__" | vercel env add "$VAR" production 2>&1 | grep -qi 'already exists'; then
    echo "  $VAR production: already exists (skip)"
  else
    echo "  $VAR production: registered"
  fi
done
```

**Note:** `PUBLIC_SITE_URL` in `.env.example` has a non-empty value (`http://localhost:4321`). Still register it as `__placeholder__` in Vercel — the real production URL gets filled in Phase 5. Preview can use the Vercel-generated preview URL; Phase 4 may override.

Verify both environments:
```bash
vercel env ls preview
vercel env ls production
```
Both must list all 8 names: `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `TURNSTILE_SITE_KEY`, `TURNSTILE_SECRET_KEY`, `GOOGLE_SHEETS_CREDENTIALS_JSON`, `GOOGLE_SHEETS_LEAD_SHEET_ID`, `SENTRY_DSN`, `PUBLIC_SITE_URL`.

**Do NOT:**
- Put real secrets in these placeholders — `__placeholder__` only. Phase 3 fills real Preview values; Phase 5 fills real Production values.
- Skip environments — both preview AND production must be populated (Pitfall 6)
- Add env var names not in `.env.example` — keep the two sources in lockstep
  </action>
  <verify>
    <automated>cd /Users/jashia/Documents/1_Projects/larraes-kitchen && EXPECTED=$(grep -oE '^[A-Z][A-Z_]+' .env.example | sort -u) && PREVIEW=$(vercel env ls preview 2>/dev/null | grep -oE '^[A-Z][A-Z_]+' | sort -u) && PRODUCTION=$(vercel env ls production 2>/dev/null | grep -oE '^[A-Z][A-Z_]+' | sort -u) && [ "$(echo "$EXPECTED" | wc -l)" = "$(echo "$PREVIEW" | wc -l)" ] && [ "$(echo "$EXPECTED" | wc -l)" = "$(echo "$PRODUCTION" | wc -l)" ] && diff <(echo "$EXPECTED") <(echo "$PREVIEW") && diff <(echo "$EXPECTED") <(echo "$PRODUCTION")</automated>
  </verify>
  <acceptance_criteria>
    - `vercel env ls preview` output contains all 8 var names from `.env.example`
    - `vercel env ls production` output contains all 8 var names from `.env.example`
    - No env var name exists in Vercel that isn't in `.env.example` (no drift)
    - All placeholder values are `__placeholder__` (not real secrets)
  </acceptance_criteria>
  <done>FND-08 satisfied: Vercel Preview and Production schemas match `.env.example` exactly. Phase 3 can replace placeholder values without adding new names.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7.5: Open a scratch PR to verify Vercel Preview + first CI run</name>
  <files>— (human verification / human action; no files modified directly by this task)</files>
  <what-built>
    First end-to-end verification that (a) GitHub↔Vercel integration creates a preview URL automatically on PR, (b) the ci.yml + pr-title.yml workflows execute and create their status check contexts (required for Task 7.6 to reference by name).
  </what-built>
  <how-to-verify>
    1. **Commit Wave 1-7 work to a scratch branch** (NOT main):
       ```bash
       git checkout -b chore/phase-1-foundation-scaffold
       git add .
       # Do NOT add .env.local (it's gitignored already)
       git status   # confirm .env.local is NOT staged
       git commit -m "chore(01): phase 1 foundation scaffold"
       git push -u origin chore/phase-1-foundation-scaffold
       ```

    2. **Open a PR** via GitHub UI or `gh pr create`:
       ```bash
       gh pr create --title "chore(01): Phase 1 foundation scaffold" --body "Phase 1 foundation — scaffold + CI + Vercel + content schemas. Plan: .planning/phases/01-foundation/"
       ```
       Title MUST start with a Conventional Commits prefix (`chore:` / `feat:` / `docs:` etc.) or the pr-title check will fail.

    3. **Verify all 6 required status checks run** (they may take a few minutes):
       - Navigate to the PR page
       - Confirm these checks appear and run: `typecheck`, `biome-check`, `content-sync`, `image-budget`, `smoke`, `pr-title`
       - All 6 should eventually pass green. If any fail, fix and re-push (do NOT proceed until all green).

    4. **Verify Vercel preview URL appears** (FND-07):
       - Vercel bot comments on the PR within ~3 minutes with a preview URL
       - Click the URL; confirm `/` renders with "Larrae's Kitchen" headline
       - Confirm the preview URL is of the form `https://<project>-<hash>-<team>.vercel.app`

    5. **Do NOT merge the PR yet** — branch protection gets configured in Task 7.6, which REQUIRES the CI contexts to have completed at least once on the repo. This PR run creates them.

    Report back once all 6 checks are green AND the Vercel preview URL is confirmed to render the site.
  </how-to-verify>
  <action>Human-driven step. Follow the instructions in `<how-to-verify>` above and use `<resume-signal>` to resume the executor.</action>
  <verify>Human approval via `<resume-signal>` response matching expected acceptance criteria.</verify>
  <done>Human has completed `<how-to-verify>` steps and replied with the `<resume-signal>` expected format.</done>
  <resume-signal>Type "6 green + preview rendering" once confirmed, OR paste error details if any check fails (we'll debug back in the relevant wave's plan before proceeding).</resume-signal>
</task>

<task type="auto">
  <name>Task 7.6: Run branch-protection script to lock main</name>
  <files>— (no repo files modified; side effect is GitHub branch-protection API)</files>
  <read_first>
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/scripts/setup-branch-protection.sh (CURRENT — from Wave 6)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-RESEARCH.md lines 926-960 (gh api recipe rationale)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-CONTEXT.md line 43 (D-11)
  </read_first>
  <action>
Now that the ci.yml + pr-title.yml contexts have been created by the Wave 7.5 scratch PR, run the branch-protection script:

```bash
cd /Users/jashia/Documents/1_Projects/larraes-kitchen
./scripts/setup-branch-protection.sh
```

Expected output: `✓ Branch protection configured.`

If the API returns an error:
- `Required status check "X" is not in a workflow triggered from this repository` → the context name in `setup-branch-protection.sh` does not match what actually ran on the scratch PR. Check the ci.yml job IDs; re-run Task 7.5 if the workflow was not yet executed.
- `Not Found` → `gh` is not authenticated or user lacks admin rights. Run `gh auth status` and `gh api user/repos/$REPO` to diagnose.
- `Resource not accessible by integration` → user lacks admin on the repo.

**Verify branch protection is applied:**
```bash
REPO=$(gh repo view --json nameWithOwner --jq '.nameWithOwner')
gh api "repos/$REPO/branches/main/protection" --jq '.required_status_checks.contexts'
```
Output MUST be a JSON array containing exactly: `["typecheck", "biome-check", "content-sync", "image-budget", "smoke", "pr-title"]` (order may vary).

```bash
gh api "repos/$REPO/branches/main/protection" --jq '.required_pull_request_reviews.require_code_owner_reviews'
```
Output MUST be `true`.

```bash
gh api "repos/$REPO/branches/main/protection" --jq '.allow_force_pushes.enabled'
```
Output MUST be `false`.

**Adversarial verification — confirm direct push to main is rejected:**
```bash
git checkout main
git pull origin main  # sync latest
# Create a trivial change
echo "# test" >> /tmp/test.txt
git commit --allow-empty -m "chore: test branch protection"
# Try to push directly to main — MUST fail
if git push origin main 2>&1 | grep -qi 'protected\|reject\|denied'; then
  echo "✓ direct push to main rejected as expected"
else
  echo "ERROR: direct push to main succeeded — protection not configured"
  exit 1
fi
# Reset the dangling commit
git reset --hard HEAD~1
```
(If local main is behind origin the empty commit may not even attempt push; the test still surfaces the protection via push error.)

**Do NOT:**
- Weaken any protection settings (e.g. lower required reviewers to 0, allow force pushes)
- Remove the scratch PR from Task 7.5 until it's merged via the normal flow (merging it becomes the first merge-through-protection check)
  </action>
  <verify>
    <automated>cd /Users/jashia/Documents/1_Projects/larraes-kitchen && REPO=$(gh repo view --json nameWithOwner --jq '.nameWithOwner') && CONTEXTS=$(gh api "repos/$REPO/branches/main/protection" --jq '.required_status_checks.contexts[]' | sort -u) && echo "$CONTEXTS" | grep -q 'typecheck' && echo "$CONTEXTS" | grep -q 'biome-check' && echo "$CONTEXTS" | grep -q 'content-sync' && echo "$CONTEXTS" | grep -q 'image-budget' && echo "$CONTEXTS" | grep -q 'smoke' && echo "$CONTEXTS" | grep -q 'pr-title' && [ "$(gh api "repos/$REPO/branches/main/protection" --jq '.required_pull_request_reviews.require_code_owner_reviews')" = "true" ] && [ "$(gh api "repos/$REPO/branches/main/protection" --jq '.allow_force_pushes.enabled')" = "false" ] && [ "$(gh api "repos/$REPO/branches/main/protection" --jq '.allow_deletions.enabled')" = "false" ]</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/setup-branch-protection.sh` exits 0 with `✓ Branch protection configured.`
    - `gh api repos/$REPO/branches/main/protection` returns 200
    - `.required_status_checks.contexts` contains exactly: `typecheck`, `biome-check`, `content-sync`, `image-budget`, `smoke`, `pr-title`
    - `.required_pull_request_reviews.require_code_owner_reviews` is `true`
    - `.required_pull_request_reviews.required_approving_review_count` is 1
    - `.allow_force_pushes.enabled` is `false`
    - `.allow_deletions.enabled` is `false`
    - Adversarial test: direct `git push origin main` from a scratch local commit is rejected by the remote
  </acceptance_criteria>
  <done>FND-06 satisfied: `main` is protected; direct pushes rejected; CODEOWNERS review required; all 6 CI contexts mandatory.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7.7: Final Phase 1 verification — merge the scratch PR through the protection gate</name>
  <files>— (human verification / human action; no files modified directly by this task)</files>
  <what-built>
    The final smoke test of the entire Phase 1 pipeline: merge the Wave 7.5 scratch PR via the normal protected-branch flow. If this works, the pipeline is live.
  </what-built>
  <how-to-verify>
    1. Return to the PR from Task 7.5.
    2. Confirm all 6 required checks still green (`typecheck`, `biome-check`, `content-sync`, `image-budget`, `smoke`, `pr-title`).
    3. Confirm the Vercel preview URL still renders `/` with "Larrae's Kitchen".
    4. As CODEOWNER (the maintainer), approve the PR via GitHub UI.
    5. Click "Merge pull request" — confirm the Merge button is enabled (not blocked by protection) and succeeds.
    6. After merge, confirm:
       - `main` now contains the Phase 1 scaffold
       - A Production deploy was triggered on Vercel (`vercel ls` or Vercel dashboard shows a new production deployment)
       - The Production deployment builds and succeeds (even with placeholder env vars — Phase 3 is where env reads happen; Phase 1 pages don't read any env vars)

    7. **Phase 1 success criteria final check — confirm each of the 5 ROADMAP bullets:**
       - ☐ 1. A fresh `git clone` builds and deploys to a Vercel preview URL on every PR without manual intervention → VERIFIED (this PR)
       - ☐ 2. Opening a PR with malformed markdown frontmatter fails CI with a clear Zod validation error before it can merge → **Adversarial proof deferred to first Phase 2 content PR (happens naturally); optional check: create a `src/content/packages/_bad.md` with invalid frontmatter on a scratch branch and confirm content-sync job fails red**
       - ☐ 3. Attempting to commit a `public/images/` file larger than 600KB fails CI with a budget-exceeded message → VERIFIED locally in Wave 6.1; **CI adversarial proof is the same 700KB test on a scratch branch — run this once as the final Phase 1 sign-off**
       - ☐ 4. `main` is protected — direct pushes are rejected, and the branch requires passing status checks plus CODEOWNERS review → VERIFIED in Task 7.6
       - ☐ 5. Warm palette and typography tokens are consumable as Tailwind utility classes in any `.astro` or `.tsx` file → VERIFIED in Wave 3 (index.astro uses `bg-surface`, `text-ink`, `font-display`, `text-display-xl`, `text-primary` and they resolve)

    8. **Optional but recommended — adversarial CI proofs** (on a fresh scratch branch, do NOT merge):
       - Add `src/content/packages/_bad.md` with `---\nid: bogus\n---\n` and push. Confirm `content-sync` job fails. Delete branch.
       - Add a 700KB file to `public/images/test.jpg` and push. Confirm `image-budget` job fails. Delete branch.

    Phase 1 passes when:
    - The scratch PR is merged green
    - Production deployment succeeds
    - (Optionally) adversarial proofs confirmed
  </how-to-verify>
  <action>Human-driven step. Follow the instructions in `<how-to-verify>` above and use `<resume-signal>` to resume the executor.</action>
  <verify>Human approval via `<resume-signal>` response matching expected acceptance criteria.</verify>
  <done>Human has completed `<how-to-verify>` steps and replied with the `<resume-signal>` expected format.</done>
  <resume-signal>Type "Phase 1 complete — production deploy green" once the PR is merged and the production Vercel deploy succeeds. Mention if you ran the optional adversarial proofs and their outcomes.</resume-signal>
</task>

</tasks>

<verification>
Wave 7 / Phase 1 final gate:
- Vercel project linked, `.vercel/project.json` committed
- All 8 env vars registered in Preview AND Production with `__placeholder__`
- Scratch PR opened, all 6 required checks pass, Vercel preview URL renders
- `main` branch protected via `scripts/setup-branch-protection.sh`; direct push rejected
- Scratch PR merged through the protection gate → Production deploy succeeds
- (Optional) Adversarial proofs: malformed content + oversized image both caught by CI
</verification>

<success_criteria>
All 9 FND-XX requirements satisfied:
- FND-01: scaffold builds and deploys (merged PR → production deploy)
- FND-02: palette tokens in Tailwind utilities (index.astro uses `bg-surface`, etc.)
- FND-03: Lovelace/Playfair/Work Sans self-hosted (no Google CDN)
- FND-04: 8 Zod-typed Content Collections (content-sync job would fail on invalid frontmatter)
- FND-05: CI runs all required checks on every PR (verified by scratch PR)
- FND-06: main protected (verified by adversarial push + gh api check)
- FND-07: Vercel preview per PR (verified by scratch PR preview URL)
- FND-08: env vars separated Preview ↔ Production (verified by `vercel env ls`)
- FND-09: image-budget rejects >600KB (verified locally + optionally adversarially on CI)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-07-SUMMARY.md` documenting:
- Vercel project ID and org ID (or note that these are in `.vercel/project.json`)
- Scratch PR URL that verified FND-07 + FND-05
- `gh api branches/main/protection` JSON output (the contexts array, reviews settings)
- Whether optional adversarial proofs were run (content-sync bad frontmatter / image-budget 700KB) and their PR URLs if so
- Confirmation that Production deployment from the scratch-PR merge succeeded
- Final checklist status for all 9 FND-XX requirements
</output>
