---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - pnpm-lock.yaml
  - node_modules
  - .nvmrc
  - .gitignore
  - astro.config.mjs
  - tsconfig.json
  - src/pages/index.astro
  - README.md
autonomous: true
requirements:
  - FND-01
must_haves:
  truths:
    - "Stale `task-master-ai` package.json is gone — repo is a clean Astro 6 project"
    - "`pnpm install --frozen-lockfile` succeeds on a fresh clone"
    - "`pnpm dev` boots an Astro dev server on http://localhost:4321 serving `/` with the word `Larrae`"
    - "Node and pnpm versions pinned: `packageManager: pnpm@9`, `engines.node >=22.12 <23`, `.nvmrc = 22`"
  artifacts:
    - path: "package.json"
      provides: "Astro 6 project manifest with packageManager + engines pins"
      contains: '"packageManager": "pnpm@9'
    - path: "astro.config.mjs"
      provides: "Base Astro config (integrations added in later waves)"
      exports: ["default"]
    - path: "tsconfig.json"
      provides: "TypeScript strict config from Astro minimal template"
      contains: '"strict": true'
    - path: "src/pages/index.astro"
      provides: "Placeholder home page with prerender=true"
      contains: 'export const prerender = true'
    - path: ".nvmrc"
      provides: "Node version pin"
      contains: "22"
  key_links:
    - from: "package.json"
      to: "node engines"
      via: "engines.node field"
      pattern: "engines.*node.*>=22\\.12"
    - from: "src/pages/index.astro"
      to: "Astro 'server' mode"
      via: "per-page prerender override"
      pattern: "export const prerender = true"
---

<objective>
Wipe the stale `task-master-ai` scaffold and initialize a greenfield Astro 6 project in the existing git repo (preserving `.git`, `.planning/`, `docs/`, `CLAUDE.md`, `.claude/`). Pin Node 22 LTS + pnpm 9 per D-17. Add `prerender = true` to the placeholder index page to prevent Pitfall 4 when later waves set `output: 'server'`.

Purpose: Every downstream wave in Phase 1 installs into the package.json produced here — wave ordering is load-bearing per RESEARCH.md § Summary. Getting the scaffold wrong now poisons every subsequent install.

Output: Clean Astro minimal-template project in repo root with version pins, dev server boots, placeholder index renders.
</objective>

<execution_context>
@/Users/jashia/Documents/1_Projects/larraes-kitchen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jashia/Documents/1_Projects/larraes-kitchen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Wipe stale scaffold and init Astro 6 minimal template</name>
  <files>package.json, package-lock.json, pnpm-lock.yaml, node_modules, .astro, .gitignore, astro.config.mjs, tsconfig.json, src/pages/index.astro, public/favicon.svg, README.md</files>
  <read_first>
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/package.json (CURRENT — contains stale task-master-ai dep to delete)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.gitignore (CURRENT — will be extended, not replaced)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-RESEARCH.md lines 170-207 (Installation sequence — canonical command set)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-RESEARCH.md lines 427-430 (Pitfall 7 — `pnpm create astro` in non-empty dir)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-CONTEXT.md lines 92-95 (Existing Code Insights — only stale dep to delete)
  </read_first>
  <action>
Execute exactly this sequence from repo root `/Users/jashia/Documents/1_Projects/larraes-kitchen`:

1. **Verify preservation targets intact before wipe** (must all exist; fail the task if any are missing):
   ```bash
   test -d .git && test -d .planning && test -d docs && test -f CLAUDE.md && test -d .claude
   ```

2. **Wipe stale scaffold only — do NOT touch anything else:**
   ```bash
   rm -rf node_modules package-lock.json package.json
   ```
   After this, `ls -la` must still show `.git/`, `.planning/`, `docs/`, `CLAUDE.md`, `.claude/`, `.gitignore`.

3. **Scaffold Astro 6 minimal template into current directory** (flags are load-bearing per Pitfall 7 — `--git no` preserves existing `.git`; `.` = current dir; `--yes` skips prompts; `--install` auto-installs deps):
   ```bash
   pnpm create astro@latest . --template minimal --install --git no --yes
   ```
   If the CLI refuses on a populated directory, fall back to scaffolding into `/tmp/astro-scaffold` then `rsync -a /tmp/astro-scaffold/ ./` (preserving existing files) — document if this fallback was needed.

4. **Pin toolchain versions (D-17).** Edit `package.json` to add at top level (merge with what Astro wrote):
   ```json
   {
     "packageManager": "pnpm@9.15.0",
     "engines": {
       "node": ">=22.12.0 <23"
     }
   }
   ```
   Use the latest pnpm 9.x available (`pnpm view pnpm@9 version` at plan time — pin the exact minor+patch to prevent lockfile drift).

5. **Create `.nvmrc`** at repo root with exactly one line:
   ```
   22
   ```

6. **Extend `.gitignore`** (append if not present — do NOT replace existing lines):
   ```
   # Astro
   .astro/
   dist/

   # Vercel
   .vercel/

   # Env
   .env
   .env.local

   # Playwright
   playwright-report/
   test-results/

   # Misc
   .cache/
   ```

7. **Update `src/pages/index.astro`** (replace Astro's default template). Contents must be:
   ```astro
   ---
   // Per RESEARCH § Pitfall 4 — prevents every page becoming an on-demand function under output:'server' (wave 2+)
   export const prerender = true;
   ---
   <html lang="en">
     <head>
       <meta charset="utf-8" />
       <meta name="viewport" content="width=device-width, initial-scale=1" />
       <title>Larrae's Kitchen</title>
     </head>
     <body>
       <h1>Larrae's Kitchen</h1>
       <p>Authentic soul food catering — site under construction.</p>
     </body>
   </html>
   ```

8. **Confirm `tsconfig.json` has `strict: true`** (Astro minimal template ships it). If missing, add `"strict": true` and `"noUncheckedIndexedAccess": true` under `compilerOptions`. Do NOT add `@/*` path alias yet — shadcn init (Wave 3) will add it cleanly.

9. **Create minimal `README.md`** at repo root:
   ```markdown
   # Larrae's Kitchen

   Marketing site for Larrae's Kitchen, a soul food caterer in Benicia, CA.

   ## Requirements
   - Node 22.12+ (see `.nvmrc`)
   - pnpm 9 (see `packageManager` in `package.json`)

   ## Commands
   - `pnpm dev` — Astro dev server on http://localhost:4321
   - `pnpm build` — Production build
   - `pnpm check:images` — Image-budget CI check (wired in a later wave)

   ## Contributing
   See `CLAUDE.md` for agent instructions and `.planning/` for roadmap, phase plans, and state.
   ```

10. **Reinstall with frozen lockfile** to generate `pnpm-lock.yaml` committed with the Astro-written deps:
    ```bash
    pnpm install
    ```

11. **Smoke-test dev server boots** (run in background, curl, kill):
    ```bash
    pnpm dev &
    DEV_PID=$!
    sleep 8
    curl -sf http://localhost:4321/ | grep -qi "larrae"
    kill $DEV_PID
    ```
    Exit non-zero if grep fails.

**Do NOT:**
- Re-init git (Astro's `--git no` prevents this; confirm `.git/` still contains pre-existing commit history: `git log --oneline | head -3`)
- Install react, mdx, sitemap, vercel, tailwind, shadcn — those land in later waves
- Install dev tooling (Biome, Vitest, Playwright, lefthook) — Wave 5
- Use `@astrojs/tailwind` at any point — forbidden by CLAUDE.md
- Add a `tailwind.config.js` — v3 idiom, forbidden
  </action>
  <verify>
    <automated>cd /Users/jashia/Documents/1_Projects/larraes-kitchen && test -f package.json && test -f astro.config.mjs && test -f tsconfig.json && test -f src/pages/index.astro && test -f .nvmrc && grep -q '"packageManager": "pnpm@9' package.json && grep -q '"node": ">=22.12' package.json && grep -qx '22' .nvmrc && grep -q 'export const prerender = true' src/pages/index.astro && grep -q '\.astro/' .gitignore && grep -q '\.vercel/' .gitignore && ! grep -q 'task-master-ai' package.json && test -d .git && test -d .planning && test -d docs && test -f CLAUDE.md && pnpm install --frozen-lockfile && (pnpm dev &) && sleep 10 && curl -sf http://localhost:4321/ | grep -qi 'larrae' && pkill -f 'astro dev' || true</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists; `grep -q 'task-master-ai' package.json` returns 1 (NOT present)
    - `grep -q '"packageManager": "pnpm@9' package.json` returns 0
    - `grep -E '"node": ">=22\.12' package.json` matches
    - `.nvmrc` contains exactly `22` (and nothing else): `grep -qx '22' .nvmrc`
    - `.gitignore` contains lines `.astro/`, `.vercel/`, `.env.local`, `playwright-report/`, `test-results/`
    - `src/pages/index.astro` contains `export const prerender = true`
    - `src/pages/index.astro` body contains the string `Larrae's Kitchen`
    - `tsconfig.json` `compilerOptions.strict` is `true`
    - `.git/` directory still present with pre-existing commits intact: `git log --oneline | grep -q 'create roadmap'` (existing commit `2b7d87f`)
    - `.planning/`, `docs/`, `CLAUDE.md`, `.claude/` all still present unchanged
    - `pnpm install --frozen-lockfile` exits 0
    - `pnpm dev` + curl localhost:4321 returns HTML containing `Larrae` (case-insensitive)
    - `node_modules/astro/package.json` reports version `6.x.x`: `node -p "require('./node_modules/astro/package.json').version"` starts with `6.`
  </acceptance_criteria>
  <done>Clean Astro 6 minimal-template scaffold in repo root; Node 22 + pnpm 9 pinned; `.git` and all planning artifacts preserved; dev server boots and serves `/` with brand word.</done>
</task>

</tasks>

<verification>
Phase 1 Wave 1 gate:
- `pnpm install --frozen-lockfile` clean
- `pnpm dev` boots, `/` returns 200 with brand word
- No `task-master-ai` anywhere in repo
- Pre-existing git history intact (check for commit `2b7d87f`)
</verification>

<success_criteria>
Task 1.1 acceptance criteria all satisfied; dev server verified; pre-existing repo artifacts untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` documenting:
- Exact Astro version installed (from `node_modules/astro/package.json`)
- Exact pnpm version pinned
- Any fallback paths taken (e.g., if `pnpm create astro .` refused and rsync fallback was used)
- Any deviations from the prescribed commands
</output>
