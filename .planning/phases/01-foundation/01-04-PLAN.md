---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-foundation-02"]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/lib/schemas/site.ts
  - src/lib/schemas/hero.ts
  - src/lib/schemas/about.ts
  - src/lib/schemas/menu.ts
  - src/lib/schemas/packages.ts
  - src/lib/schemas/testimonials.ts
  - src/lib/schemas/faq.ts
  - src/lib/schemas/gallery.ts
  - src/content.config.ts
  - src/lib/pricing/estimate.ts
  - src/lib/pricing/estimate.test.ts
  - src/content/site/.gitkeep
  - src/content/hero/.gitkeep
  - src/content/about/.gitkeep
  - src/content/menu/.gitkeep
  - src/content/packages/.gitkeep
  - src/content/testimonials/.gitkeep
  - src/content/faq/.gitkeep
  - src/content/gallery/.gitkeep
  - src/actions/.gitkeep
  - src/layouts/.gitkeep
  - src/components/sections/.gitkeep
  - src/lib/email/.gitkeep
  - src/lib/leads/.gitkeep
  - public/images/.gitkeep
autonomous: true
requirements:
  - FND-04
must_haves:
  truths:
    - "Eight Zod schema modules exist at `src/lib/schemas/{site,hero,about,menu,packages,testimonials,faq,gallery}.ts` — each exports a schema AND an inferred type"
    - "`src/content.config.ts` imports all 8 schemas and registers collections with the glob loader"
    - "`pnpm exec astro sync` exits 0 — Zod-validated types are emitted for all 8 collections (empty dirs produce empty collections)"
    - "Pricing stub at `src/lib/pricing/estimate.ts` exists as typed pure function that throws 'Phase 3' — import path is stable for Phase 3 wizard"
    - "Full repo directory skeleton stood up per D-07 with `.gitkeep` placeholders — Phase 2/3 PRs only add into existing dirs"
  artifacts:
    - path: "src/lib/schemas/packages.ts"
      provides: "Canonical packageSchema template (other 7 follow same pattern)"
      exports: ["packageSchema", "PackageData"]
      contains: "z.object"
    - path: "src/content.config.ts"
      provides: "Content Collections registration for all 8 domains"
      exports: ["collections"]
      contains: "defineCollection"
    - path: "src/lib/pricing/estimate.ts"
      provides: "Typed pure function stub for Phase 3 price estimator"
      exports: ["estimate", "EstimateInput", "EstimateRange"]
  key_links:
    - from: "src/content.config.ts"
      to: "src/lib/schemas/*.ts"
      via: "imports 8 schema modules"
      pattern: 'from "\./lib/schemas/'
    - from: "src/lib/pricing/estimate.ts"
      to: "src/lib/schemas/packages.ts"
      via: "imports PackageData type"
      pattern: "PackageData"
---

<objective>
Create the full content schema contract and repo directory skeleton per D-05, D-06, D-07. Eight Zod schema modules live at `src/lib/schemas/<domain>.ts`, each exporting both the schema and its inferred type. `src/content.config.ts` registers all 8 Content Collections with the glob loader. `src/lib/pricing/estimate.ts` lands as a typed stub that imports `PackageData` from the packages schema — setting the contract Phase 3's wizard imports. All placeholder directories get `.gitkeep` so `astro sync` traverses cleanly.

Purpose: This schema contract is the single source of truth referenced by Phase 2 (content authoring validates against it) and Phase 3 (wizard form + Astro Action + React Email templates import the same schemas). Getting schema shapes right now prevents Phase 2/3 churn.

Output: 8 schema files + `content.config.ts` + pricing stub + full `.gitkeep` skeleton; `astro sync` exits 0 and emits `astro:content` types for all 8 collections.
</objective>

<execution_context>
@/Users/jashia/Documents/1_Projects/larraes-kitchen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jashia/Documents/1_Projects/larraes-kitchen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 4.1: Author 8 Zod schema modules + pricing stub</name>
  <files>src/lib/schemas/site.ts, src/lib/schemas/hero.ts, src/lib/schemas/about.ts, src/lib/schemas/menu.ts, src/lib/schemas/packages.ts, src/lib/schemas/testimonials.ts, src/lib/schemas/faq.ts, src/lib/schemas/gallery.ts, src/lib/pricing/estimate.ts, src/lib/pricing/estimate.test.ts, package.json</files>
  <behavior>
    - `packageSchema.parse({id:"medium", name:"Medium", guestRange:{min:21,max:30}, pricePerPerson:{min:32,max:45}, includes:["Mains"], popular:true, order:2})` succeeds.
    - `packageSchema.parse({id:"medium", name:"Medium", guestRange:{min:30,max:21}, ...})` throws (min > max refinement).
    - `packageSchema.parse({id:"xl", ...})` throws (id not in enum).
    - Every schema module exports `<domain>Schema` AND `<DomainType>` inferred type.
    - `estimate({...})` throws `Error("estimate() not yet implemented — Phase 3")` — deliberate so accidental prod calls fail fast per D-06.
  </behavior>
  <read_first>
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-RESEARCH.md lines 518-539 (canonical packageSchema)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-RESEARCH.md lines 598-621 (canonical estimate.ts stub)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-RESEARCH.md lines 625-632 (canonical estimate.test.ts scaffold)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/REQUIREMENTS.md lines 24-33 (CONT-01..09 field expectations)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/REQUIREMENTS.md lines 36-102 (HERO, ABOUT, MENU, PKG, GAL, TEST, FAQ, CONTACT field expectations)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-CONTEXT.md lines 23-25 (D-05, D-06)
  </read_first>
  <action>
1. **Install zod** (Astro scaffold does not include it):
   ```bash
   pnpm add zod
   ```

2. **Create all 8 schema modules.** Each file exports both `<domain>Schema` and the inferred `<Domain>Type`. Use EXACT content below. Field shapes derive from REQUIREMENTS.md CONT-01..09 + section requirements.

**`src/lib/schemas/site.ts`** (CONT-01 — NAP + service area + hours + social):
```typescript
import { z } from "zod";

export const siteSchema = z.object({
  name: z.string(),
  address: z.object({
    street: z.string(),
    city: z.string(),
    region: z.string(),
    postalCode: z.string(),
    country: z.string().default("US"),
  }),
  phone: z.string(),
  email: z.string().email(),
  serviceArea: z.array(z.string()).min(1),
  hours: z.array(z.object({
    days: z.string(),
    open: z.string(),
    close: z.string(),
  })),
  social: z.object({
    instagram: z.string().url().optional(),
    facebook: z.string().url().optional(),
    google: z.string().url().optional(),
  }).default({}),
  leadTimeDays: z.number().int().positive().default(7),
  blackoutDates: z.array(z.string()).default([]),
  responseTime: z.string().default("We respond within 24 hours"),
});

export type SiteData = z.infer<typeof siteSchema>;
```

**`src/lib/schemas/hero.ts`** (CONT-08, HERO-01..05):
```typescript
import { z } from "zod";

export const heroSchema = z.object({
  headline: z.string(),
  subheadline: z.string().optional(),
  ctaText: z.string(),
  priceChip: z.string(),
  heroImage: z.string(),
  heroImageAlt: z.string(),
});

export type HeroData = z.infer<typeof heroSchema>;
```

**`src/lib/schemas/about.ts`** (ABOUT-01..03):
```typescript
import { z } from "zod";

export const aboutSchema = z.object({
  heritageNarrative: z.string().min(150).max(2500),
  positioning: z.string(),
  chefPortrait: z.string().optional(),
  chefPortraitAlt: z.string().optional(),
});

export type AboutData = z.infer<typeof aboutSchema>;
```

**`src/lib/schemas/menu.ts`** (MENU-01..04):
```typescript
import { z } from "zod";

export const menuItemSchema = z.object({
  name: z.string(),
  category: z.enum(["proteins", "sides", "desserts"]),
  description: z.string(),
  dietary: z.array(z.enum([
    "vegetarian", "vegan", "gluten-free", "dairy-free", "nut-free",
  ])).default([]),
  photo: z.string().optional(),
  photoAlt: z.string().optional(),
  order: z.number().int(),
});

export type MenuItem = z.infer<typeof menuItemSchema>;
```

**`src/lib/schemas/packages.ts`** (PKG-01..05, CONT-02/03 — canonical per RESEARCH):
```typescript
// Source: CONTEXT D-05, D-06 + RESEARCH § Code Examples. Canonical template.
import { z } from "zod";

export const packageSchema = z.object({
  id: z.enum(["small", "medium", "large"]),
  name: z.string(),
  guestRange: z.object({
    min: z.number().int().positive(),
    max: z.number().int().positive(),
  }).refine((r) => r.min <= r.max, "min must be <= max"),
  pricePerPerson: z.object({
    min: z.number().positive(),
    max: z.number().positive(),
  }).refine((r) => r.min <= r.max, "min must be <= max"),
  includes: z.array(z.string()).min(1),
  popular: z.boolean().default(false),
  order: z.number().int(),
});

export type PackageData = z.infer<typeof packageSchema>;
```

**`src/lib/schemas/testimonials.ts`** (TEST-01..04):
```typescript
import { z } from "zod";

export const testimonialSchema = z.object({
  clientName: z.string(),
  eventType: z.enum(["family", "social", "corporate", "other"]),
  quote: z.string(),
  rating: z.number().int().min(1).max(5),
  order: z.number().int(),
});

export type TestimonialData = z.infer<typeof testimonialSchema>;
```

**`src/lib/schemas/faq.ts`** (FAQ-01..04):
```typescript
import { z } from "zod";

export const faqGroupSchema = z.object({
  category: z.enum(["ordering", "delivery", "menu-customization", "payment"]),
  order: z.number().int(),
  questions: z.array(z.object({
    question: z.string(),
    answer: z.string(),
  })).min(1),
});

export type FaqGroup = z.infer<typeof faqGroupSchema>;
```

**`src/lib/schemas/gallery.ts`** (GAL-01..06):
```typescript
import { z } from "zod";

export const gallerySchema = z.object({
  image: z.string(),
  alt: z.string(),
  caption: z.string().optional(),
  aspectRatio: z.enum(["1:1", "4:3", "3:2", "16:9", "3:4", "2:3", "9:16"]),
  order: z.number().int(),
});

export type GalleryImage = z.infer<typeof gallerySchema>;
```

3. **Create `src/lib/pricing/estimate.ts`** — lift byte-for-byte from RESEARCH § Code Examples lines 598-621:
```typescript
// Source: CONTEXT D-06 — typed stub; real implementation lands in Phase 3
import type { PackageData } from "../schemas/packages";

export interface EstimateInput {
  guests: number;
  packageId: PackageData["id"];
  packages: readonly PackageData[];
}

export interface EstimateRange {
  min: number;
  max: number;
}

/**
 * Pure, deterministic price estimator. Returns a range for the given guest
 * count against the pre-validated package data. Phase 3 will implement this.
 */
export function estimate(_input: EstimateInput): EstimateRange | null {
  // Phase 3 stub — deliberate throw so any accidental production call fails fast.
  throw new Error("estimate() not yet implemented — Phase 3");
}
```

4. **Create `src/lib/pricing/estimate.test.ts`** — lift from RESEARCH lines 625-632:
```typescript
import { describe, it } from "vitest";

describe("estimate()", () => {
  // Real boundary matrix (EST-05/EST-06) lands in Phase 3. Scaffold only.
  it.skip("returns a range for every tier boundary (Phase 3)", () => {});
});
```

   Note: `vitest` is not installed yet (Wave 5 installs it). `estimate.test.ts` will show a "cannot find module 'vitest'" diagnostic until Wave 5. That is expected.

**Do NOT:**
- Author real content markdown files — Phase 2's job (`.gitkeep` only)
- Implement the real `estimate()` function — Phase 3
- Add fields beyond those listed above — keep schemas minimal
- Omit the `.refine()` checks on `packageSchema` ranges — they catch malformed content at CI time
  </action>
  <verify>
    <automated>cd /Users/jashia/Documents/1_Projects/larraes-kitchen && grep -q '"zod"' package.json && test -f src/lib/schemas/site.ts && test -f src/lib/schemas/hero.ts && test -f src/lib/schemas/about.ts && test -f src/lib/schemas/menu.ts && test -f src/lib/schemas/packages.ts && test -f src/lib/schemas/testimonials.ts && test -f src/lib/schemas/faq.ts && test -f src/lib/schemas/gallery.ts && grep -q 'export const siteSchema' src/lib/schemas/site.ts && grep -q 'export const heroSchema' src/lib/schemas/hero.ts && grep -q 'export const aboutSchema' src/lib/schemas/about.ts && grep -q 'export const menuItemSchema' src/lib/schemas/menu.ts && grep -q 'export const packageSchema' src/lib/schemas/packages.ts && grep -q 'export const testimonialSchema' src/lib/schemas/testimonials.ts && grep -q 'export const faqGroupSchema' src/lib/schemas/faq.ts && grep -q 'export const gallerySchema' src/lib/schemas/gallery.ts && grep -q 'export type PackageData' src/lib/schemas/packages.ts && grep -q 'export type SiteData' src/lib/schemas/site.ts && test -f src/lib/pricing/estimate.ts && grep -q 'export function estimate' src/lib/pricing/estimate.ts && grep -q 'Phase 3' src/lib/pricing/estimate.ts && test -f src/lib/pricing/estimate.test.ts && grep -q 'it.skip' src/lib/pricing/estimate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm list zod` shows zod installed (3.x or 4.x)
    - All 8 schema files exist at `src/lib/schemas/{site,hero,about,menu,packages,testimonials,faq,gallery}.ts`
    - Each schema file exports its named schema: `siteSchema`, `heroSchema`, `aboutSchema`, `menuItemSchema`, `packageSchema`, `testimonialSchema`, `faqGroupSchema`, `gallerySchema`
    - Each schema file exports inferred type: `SiteData`, `HeroData`, `AboutData`, `MenuItem`, `PackageData`, `TestimonialData`, `FaqGroup`, `GalleryImage`
    - `packageSchema` contains both `.refine()` checks for `guestRange` and `pricePerPerson`
    - `packageSchema.id` is `z.enum(["small", "medium", "large"])` — matches CONT-03
    - `src/lib/pricing/estimate.ts` exports `estimate`, `EstimateInput`, `EstimateRange`
    - `estimate.ts` imports `PackageData` from `../schemas/packages`
    - `estimate.ts` throws with error message containing "Phase 3"
    - `src/lib/pricing/estimate.test.ts` exists with at least one `it.skip`
    - No real content markdown files under `src/content/*/` (Phase 2 concern)
  </acceptance_criteria>
  <done>All 8 Zod schemas + pricing stub authored; schema modules export both schema and inferred type; `PackageData` import path stable for Phase 3.</done>
</task>

<task type="auto">
  <name>Task 4.2: Wire Content Collections config + author repo directory skeleton</name>
  <files>src/content.config.ts, src/content/site/.gitkeep, src/content/hero/.gitkeep, src/content/about/.gitkeep, src/content/menu/.gitkeep, src/content/packages/.gitkeep, src/content/testimonials/.gitkeep, src/content/faq/.gitkeep, src/content/gallery/.gitkeep, src/actions/.gitkeep, src/layouts/.gitkeep, src/components/sections/.gitkeep, src/lib/email/.gitkeep, src/lib/leads/.gitkeep, public/images/.gitkeep</files>
  <read_first>
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-RESEARCH.md lines 545-594 (canonical src/content.config.ts)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-RESEARCH.md lines 402-406 (Pitfall 3 — empty dir / astro sync behavior)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-CONTEXT.md lines 26-37 (D-07 repo skeleton)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/src/lib/schemas/packages.ts (created in Task 4.1 — imports must match)
  </read_first>
  <action>
1. **Create `src/content.config.ts`** with EXACT contents (lifted from RESEARCH lines 545-592):
```typescript
// Source: docs.astro.build/en/guides/content-collections/ + CONTEXT D-05
import { defineCollection } from "astro:content";
import { glob } from "astro/loaders";
import { siteSchema } from "./lib/schemas/site";
import { heroSchema } from "./lib/schemas/hero";
import { aboutSchema } from "./lib/schemas/about";
import { menuItemSchema } from "./lib/schemas/menu";
import { packageSchema } from "./lib/schemas/packages";
import { testimonialSchema } from "./lib/schemas/testimonials";
import { faqGroupSchema } from "./lib/schemas/faq";
import { gallerySchema } from "./lib/schemas/gallery";

const site = defineCollection({
  loader: glob({ base: "./src/content/site", pattern: "**/*.md" }),
  schema: siteSchema,
});
const hero = defineCollection({
  loader: glob({ base: "./src/content/hero", pattern: "**/*.md" }),
  schema: heroSchema,
});
const about = defineCollection({
  loader: glob({ base: "./src/content/about", pattern: "**/*.md" }),
  schema: aboutSchema,
});
const menu = defineCollection({
  loader: glob({ base: "./src/content/menu", pattern: "**/*.md" }),
  schema: menuItemSchema,
});
const packages = defineCollection({
  loader: glob({ base: "./src/content/packages", pattern: "**/*.md" }),
  schema: packageSchema,
});
const testimonials = defineCollection({
  loader: glob({ base: "./src/content/testimonials", pattern: "**/*.md" }),
  schema: testimonialSchema,
});
const faq = defineCollection({
  loader: glob({ base: "./src/content/faq", pattern: "**/*.md" }),
  schema: faqGroupSchema,
});
const gallery = defineCollection({
  loader: glob({ base: "./src/content/gallery", pattern: "**/*.md" }),
  schema: gallerySchema,
});

export const collections = { site, hero, about, menu, packages, testimonials, faq, gallery };
```

2. **Create all 8 content directories with `.gitkeep` placeholders** (D-07):
```bash
for dir in site hero about menu packages testimonials faq gallery; do
  mkdir -p "src/content/$dir"
  touch "src/content/$dir/.gitkeep"
done
```

3. **Create remaining skeleton directories** (D-07 — housing for Phase 2/3 output):
```bash
mkdir -p src/actions && touch src/actions/.gitkeep
mkdir -p src/layouts && touch src/layouts/.gitkeep
mkdir -p src/components/sections && touch src/components/sections/.gitkeep
mkdir -p src/lib/email && touch src/lib/email/.gitkeep
mkdir -p src/lib/leads && touch src/lib/leads/.gitkeep
mkdir -p public/images && touch public/images/.gitkeep
```
`src/components/ui/` already exists (Wave 3 shadcn). `src/lib/utils.ts` already exists. `src/lib/pricing/` already has files from Task 4.1.

4. **Run `astro sync` and assert success.** Per Pitfall 3, empty dirs can fail sync on some Astro versions. Expected behavior (per RESEARCH line 594): glob returns `[]` and sync emits empty collection types.

   ```bash
   pnpm exec astro sync
   ```
   Must exit 0.

   **Fallback (if Pitfall 3 hits — Astro errors "collection does not exist"):** Seed `_placeholder.md` in each failing collection dir with frontmatter-only body:
   ```
   ---
   ---
   ```
   Note: placeholder markdown will fail Zod validation (missing required fields). Use this fallback ONLY if empty-dir sync fails, and document in SUMMARY. Preferred path is empty-dir success per RESEARCH line 594. Do NOT remove `.gitkeep`.

5. **Confirm generated types.** After sync, `.astro/content.d.ts` (or `.astro/types.d.ts`) should reference all 8 collection names:
```bash
grep -E '"site"|"hero"|"about"|"menu"|"packages"|"testimonials"|"faq"|"gallery"' .astro/content*.d.ts .astro/types.d.ts 2>/dev/null | head -20
```
All 8 collection names should appear.

6. **Confirm `astro check` still exits 0:**
```bash
pnpm exec astro check
```

**Do NOT:**
- Add content markdown in any `src/content/*/` dir — Phase 2's job
- Omit any of the 8 collections from `content.config.ts` — must match the 8 schemas exactly
- Place `content.config.ts` at `src/content/config.ts` — CLAUDE.md / RESEARCH § State of the Art mandates the Astro 5+ location `src/content.config.ts`
- Use `_placeholder.md` as the default — that's only a fallback for Pitfall 3
  </action>
  <verify>
    <automated>cd /Users/jashia/Documents/1_Projects/larraes-kitchen && test -f src/content.config.ts && grep -q 'defineCollection' src/content.config.ts && grep -q 'siteSchema' src/content.config.ts && grep -q 'heroSchema' src/content.config.ts && grep -q 'aboutSchema' src/content.config.ts && grep -q 'menuItemSchema' src/content.config.ts && grep -q 'packageSchema' src/content.config.ts && grep -q 'testimonialSchema' src/content.config.ts && grep -q 'faqGroupSchema' src/content.config.ts && grep -q 'gallerySchema' src/content.config.ts && grep -q 'export const collections' src/content.config.ts && ! test -f src/content/config.ts && for d in site hero about menu packages testimonials faq gallery; do test -f "src/content/$d/.gitkeep" || exit 1; done && test -f src/actions/.gitkeep && test -f src/layouts/.gitkeep && test -f src/components/sections/.gitkeep && test -f src/lib/email/.gitkeep && test -f src/lib/leads/.gitkeep && test -f public/images/.gitkeep && pnpm exec astro sync</automated>
  </verify>
  <acceptance_criteria>
    - `src/content.config.ts` exists at the Astro 5+ canonical path (NOT `src/content/config.ts`)
    - Imports all 8 schemas: `siteSchema`, `heroSchema`, `aboutSchema`, `menuItemSchema`, `packageSchema`, `testimonialSchema`, `faqGroupSchema`, `gallerySchema`
    - Exports `collections` object with all 8 keys: `site, hero, about, menu, packages, testimonials, faq, gallery`
    - All 8 content directories exist with `.gitkeep`: `src/content/{site,hero,about,menu,packages,testimonials,faq,gallery}/.gitkeep`
    - All 6 skeleton directories exist with `.gitkeep`: `src/actions/.gitkeep`, `src/layouts/.gitkeep`, `src/components/sections/.gitkeep`, `src/lib/email/.gitkeep`, `src/lib/leads/.gitkeep`, `public/images/.gitkeep`
    - `pnpm exec astro sync` exits 0
    - `.astro/content.d.ts` (or equivalent) contains references to all 8 collection names
    - `pnpm exec astro check` exits 0 (only expected warning: vitest import in `estimate.test.ts` unresolved — acceptable, Wave 5 fixes)
  </acceptance_criteria>
  <done>Content Collections wired for all 8 domains; `astro sync` emits typed content; repo skeleton ready for Phase 2/3 content and code.</done>
</task>

</tasks>

<verification>
Wave 4 gate:
- 8 schemas present with schema + type exports
- `content.config.ts` at correct Astro 5+ path, registers all 8 collections
- `astro sync` exits 0
- Full D-07 directory skeleton stood up
</verification>

<success_criteria>
All task acceptance criteria satisfied; Phase 2 can author content and Phase 3 can import schemas against stable contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md` documenting:
- Zod major version installed
- Whether Pitfall 3 fallback was needed (`_placeholder.md` seeding) for any collection
- Output of `ls -la .astro/content.d.ts` confirming generated types file exists
- Any deviations from canonical schemas
</output>
