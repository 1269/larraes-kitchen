---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-foundation-02"]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - astro.config.mjs
  - src/styles/global.css
  - src/pages/index.astro
  - tsconfig.json
  - components.json
  - src/lib/utils.ts
  - src/components/ui/button.tsx
  - public/fonts/Lovelace.woff2
  - public/fonts/LOVELACE-LICENSE.txt
autonomous: false
requirements:
  - FND-01
  - FND-02
  - FND-03
must_haves:
  truths:
    - "Tailwind v4 is wired via `@tailwindcss/vite` — NOT `@astrojs/tailwind`"
    - "`src/styles/global.css` declares full warm palette + semantic layer + editorial typography scale in `@theme`"
    - "`bg-primary`, `text-ink`, `font-display`, `text-display-xl` resolve as valid Tailwind utilities"
    - "Playfair Display + Work Sans load from Fontsource (self-hosted via npm); Lovelace loads from `/public/fonts/Lovelace.woff2` with license doc"
    - "shadcn/ui is initialized and the Button component is installed"
    - "Zero requests to fonts.googleapis.com at runtime"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Tailwind import + @theme tokens + Lovelace @font-face"
      contains: "@theme"
    - path: "astro.config.mjs"
      provides: "Tailwind Vite plugin wired alongside integrations"
      contains: "tailwindcss()"
    - path: "components.json"
      provides: "shadcn config"
      exports: []
    - path: "src/lib/utils.ts"
      provides: "cn() helper from shadcn init"
      exports: ["cn"]
    - path: "src/components/ui/button.tsx"
      provides: "shadcn Button component"
      exports: ["Button", "buttonVariants"]
    - path: "public/fonts/Lovelace.woff2"
      provides: "Commercial display font file"
    - path: "public/fonts/LOVELACE-LICENSE.txt"
      provides: "License documentation for Lovelace"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/styles/global.css"
      via: "layout/page import"
      pattern: "import.*styles/global\\.css"
    - from: "src/styles/global.css"
      to: "Tailwind v4 runtime"
      via: '@import "tailwindcss"'
      pattern: '@import "tailwindcss"'
    - from: "components.json"
      to: "src/lib/utils.ts"
      via: "shadcn config aliases"
      pattern: '"utils":\\s*"@/lib/utils"'
---

<objective>
Wire Tailwind v4 via `@tailwindcss/vite` (NOT `@astrojs/tailwind`). Author `src/styles/global.css` with the full brand + semantic + typography token layers per D-01..D-04. Install Fontsource self-hosted fonts (Playfair Display + Work Sans) and self-host the commercial Lovelace font with license documentation. Initialize shadcn/ui and install the Button component (D-08).

Purpose: Design tokens become Tailwind utility classes Phase 2 consumes. The two-layer model (brand + semantic) means a future palette swap only touches the semantic layer. Fonts self-hosted = no Google CDN hop = FND-03 + PERF-01 protected. shadcn Button is used in every Phase 2 section (hero CTA, package cards, FAQ) and Phase 3 wizard — installing now prevents downstream churn.

Output: Tokens compile to CSS custom properties in production bundle; `font-display` / `text-display-xl` / `bg-primary` all work; `Button` importable from `@/components/ui/button`.
</objective>

<execution_context>
@/Users/jashia/Documents/1_Projects/larraes-kitchen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jashia/Documents/1_Projects/larraes-kitchen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Install Tailwind v4 + author global.css with tokens, install Fontsource + Lovelace</name>
  <files>package.json, pnpm-lock.yaml, astro.config.mjs, src/styles/global.css, src/pages/index.astro, public/fonts/LOVELACE-LICENSE.txt</files>
  <read_first>
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/astro.config.mjs (CURRENT — extend vite config, do not remove integrations)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/src/pages/index.astro (CURRENT — need to import global.css)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-RESEARCH.md lines 184-189 (Tailwind + Fontsource install commands)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-RESEARCH.md lines 442-464 (full astro.config.mjs with tailwindcss() in vite.plugins)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-RESEARCH.md lines 466-513 (canonical src/styles/global.css — lift byte-for-byte)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-RESEARCH.md lines 390-394 (Pitfall 1 — do NOT use @astrojs/tailwind)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-RESEARCH.md lines 432-436 (Pitfall 8 — Lovelace license)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-CONTEXT.md lines 16-20 (D-01, D-02, D-03, D-04 — token model)
  </read_first>
  <action>
1. **Install Tailwind v4 + Vite plugin** (do NOT use `astro add tailwind` — that installs the v3 integration per Pitfall 1):
   ```bash
   pnpm add tailwindcss @tailwindcss/vite
   ```
   Verify both land in `dependencies`. Confirm majors: `pnpm list tailwindcss @tailwindcss/vite` should show 4.x.

2. **Install Fontsource fonts** (D-03):
   ```bash
   pnpm add @fontsource/playfair-display @fontsource/work-sans
   ```

3. **Edit `astro.config.mjs`** — add the `vite.plugins` stanza. The file must now match RESEARCH § Code Examples lines 442-464 exactly:
   ```javascript
   // @ts-check
   import { defineConfig } from "astro/config";
   import react from "@astrojs/react";
   import mdx from "@astrojs/mdx";
   import sitemap from "@astrojs/sitemap";
   import vercel from "@astrojs/vercel";
   import tailwindcss from "@tailwindcss/vite";

   // https://astro.build/config
   export default defineConfig({
     site: "https://larraeskitchen.com",
     output: "server",
     adapter: vercel({
       webAnalytics: { enabled: false },
       imageService: true,
     }),
     integrations: [react(), mdx(), sitemap()],
     vite: {
       plugins: [tailwindcss()],
     },
   });
   ```

4. **Create `src/styles/global.css`** with EXACT contents from RESEARCH § Code Examples lines 466-513:
   ```css
   /* Source: tailwindcss.com/docs/theme + CONTEXT D-01..D-04 */
   @import "tailwindcss";

   /* Fontsource CSS is imported from JS (see src/pages/index.astro or layout).
      Lovelace is commercial; self-hosted below. License: /public/fonts/LOVELACE-LICENSE.txt */
   @font-face {
     font-family: "Lovelace";
     src: url("/fonts/Lovelace.woff2") format("woff2");
     font-weight: 400;
     font-style: normal;
     font-display: swap;
   }

   @theme {
     /* ─── Brand layer (D-01, D-02) ─── */
     --color-deep-amber: #B8621B;
     --color-warm-cream: #F7EFD9;
     --color-greens-deep: #2E4A2F;
     --color-greens-mid: #4A6B4C;
     --color-iron-black: #1C1B19;
     --color-southern-red: #9F2F2A;
     --color-butter-gold: #E8C36A;
     --color-clay: #B5775E;

     /* ─── Semantic layer (D-01) ─── */
     --color-primary: var(--color-greens-deep);
     --color-surface: var(--color-warm-cream);
     --color-accent: var(--color-deep-amber);
     --color-ink: var(--color-iron-black);

     /* ─── Typography (D-03, D-04) ─── */
     --font-display: "Lovelace", "Playfair Display", ui-serif, Georgia, serif;
     --font-serif: "Playfair Display", ui-serif, Georgia, serif;
     --font-sans: "Work Sans", ui-sans-serif, system-ui, -apple-system, sans-serif;

     --text-display-xl: 4.5rem;
     --text-display-xl--line-height: 1.05;
     --text-display-lg: 3rem;
     --text-display-md: 2rem;
     --text-body-lg: 1.125rem;
     --text-body-md: 1rem;
     --text-body-sm: 0.875rem;
     --text-eyebrow: 0.75rem;
     --text-eyebrow--letter-spacing: 0.12em;
   }
   ```

5. **Update `src/pages/index.astro`** — add Fontsource + global.css imports to the frontmatter, and use a utility class to prove tokens resolve. Exact contents:
   ```astro
   ---
   export const prerender = true;
   import "@fontsource/playfair-display/400.css";
   import "@fontsource/playfair-display/700.css";
   import "@fontsource/work-sans/400.css";
   import "@fontsource/work-sans/600.css";
   import "../styles/global.css";
   ---
   <html lang="en">
     <head>
       <meta charset="utf-8" />
       <meta name="viewport" content="width=device-width, initial-scale=1" />
       <title>Larrae's Kitchen</title>
     </head>
     <body class="bg-surface text-ink font-sans">
       <h1 class="font-display text-display-xl text-primary">Larrae's Kitchen</h1>
       <p class="font-serif">Authentic soul food catering — site under construction.</p>
     </body>
   </html>
   ```

6. **Lovelace font — license documentation and placeholder file.** The actual `.woff2` is a commercial asset the maintainer must supply (per Assumption A3 in RESEARCH lines 1008-1010). For Phase 1:
   - Create `public/fonts/LOVELACE-LICENSE.txt` with placeholder content:
     ```
     Lovelace Font — License Documentation

     PENDING: Maintainer must paste the license terms from the
     purchase receipt (Set Sail Studios / Ellen Luff) into this file
     before the production deploy (Phase 5).

     The @font-face declaration in src/styles/global.css references
     /fonts/Lovelace.woff2 — the actual font file must be committed
     alongside this license before Phase 5 launch.

     If Lovelace is not licensed for web use, fall back to Playfair Display
     for display type (RESEARCH § Assumption A3 fallback).
     ```
   - If `public/fonts/Lovelace.woff2` does NOT exist yet, that is acceptable for Phase 1 — the `@font-face` will 404 gracefully and the font-family stack in `--font-display` falls back to `"Playfair Display", ui-serif, Georgia, serif`. Document in plan SUMMARY that Lovelace.woff2 is pending maintainer supply. Do NOT create a fake/empty `.woff2` binary.

7. **Build and verify tokens compile to CSS.**
   ```bash
   pnpm build
   ```
   Then grep the built CSS bundle for the brand color variable:
   ```bash
   grep -r 'color-deep-amber' dist/ || grep -r 'color-deep-amber' .vercel/output/static/ 2>/dev/null
   ```
   Exit non-zero if the token name is absent from the built CSS — that means Tailwind v4's `@theme` did not compile (Pitfall 1 signal).

8. **Dev server smoke.** Confirm `/` still renders and h1 has computed `font-family` matching Playfair (Lovelace may 404 and fall back; that's fine for Phase 1):
   ```bash
   pnpm dev &
   DEV_PID=$!
   sleep 10
   curl -sf http://localhost:4321/ | grep -qi "larrae"
   kill $DEV_PID
   ```

**Do NOT:**
- Install `@astrojs/tailwind` — forbidden by CLAUDE.md + Pitfall 1
- Create `tailwind.config.js` — v3 idiom, explicitly forbidden
- Load fonts from `fonts.googleapis.com` — per D-03 and FND-03
- Create a fake/zero-byte `Lovelace.woff2` — it's acceptable for the font to 404 and fall back to Playfair in Phase 1; the maintainer supplies the real file later
  </action>
  <verify>
    <automated>cd /Users/jashia/Documents/1_Projects/larraes-kitchen && grep -q '"tailwindcss"' package.json && grep -q '"@tailwindcss/vite"' package.json && grep -q '"@fontsource/playfair-display"' package.json && grep -q '"@fontsource/work-sans"' package.json && ! grep -q '@astrojs/tailwind' package.json && grep -q 'import tailwindcss from "@tailwindcss/vite"' astro.config.mjs && grep -q 'tailwindcss()' astro.config.mjs && test -f src/styles/global.css && grep -q '@import "tailwindcss"' src/styles/global.css && grep -q '@theme' src/styles/global.css && grep -q 'color-deep-amber' src/styles/global.css && grep -q 'color-greens-deep' src/styles/global.css && grep -q 'color-primary' src/styles/global.css && grep -q 'font-display' src/styles/global.css && grep -q 'text-display-xl' src/styles/global.css && grep -q 'Lovelace' src/styles/global.css && grep -q '@fontsource/playfair-display' src/pages/index.astro && grep -q 'global.css' src/pages/index.astro && test -f public/fonts/LOVELACE-LICENSE.txt && ! test -f tailwind.config.js && ! test -f tailwind.config.ts && pnpm build && (grep -rq 'color-deep-amber' dist/ 2>/dev/null || grep -rq 'color-deep-amber' .vercel/output/static/ 2>/dev/null)</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` has `tailwindcss`, `@tailwindcss/vite`, `@fontsource/playfair-display`, `@fontsource/work-sans` — all present
    - `package.json` does NOT have `@astrojs/tailwind`
    - No `tailwind.config.js` or `tailwind.config.ts` anywhere in repo
    - `astro.config.mjs` imports `tailwindcss` from `@tailwindcss/vite` and adds it to `vite.plugins`
    - `src/styles/global.css` contains: `@import "tailwindcss";`, `@font-face` for Lovelace pointing to `/fonts/Lovelace.woff2`, `@theme` block
    - `@theme` block contains all 8 brand color vars: `--color-deep-amber`, `--color-warm-cream`, `--color-greens-deep`, `--color-greens-mid`, `--color-iron-black`, `--color-southern-red`, `--color-butter-gold`, `--color-clay`
    - `@theme` block contains all 4 semantic vars: `--color-primary`, `--color-surface`, `--color-accent`, `--color-ink`
    - `@theme` block contains all 3 font vars: `--font-display`, `--font-serif`, `--font-sans`
    - `@theme` block contains editorial text scale: `--text-display-xl`, `--text-display-lg`, `--text-display-md`, `--text-body-lg`, `--text-body-md`, `--text-body-sm`, `--text-eyebrow`
    - `src/pages/index.astro` imports Fontsource CSS and `../styles/global.css`
    - `src/pages/index.astro` uses token-backed utilities: `bg-surface`, `text-ink`, `font-sans`, `font-display`, `text-display-xl`, `text-primary`
    - `public/fonts/LOVELACE-LICENSE.txt` exists with placeholder license text
    - `pnpm build` exits 0
    - Built CSS bundle (under `dist/` or `.vercel/output/static/`) contains the string `color-deep-amber` — proves Tailwind v4 `@theme` compiled
  </acceptance_criteria>
  <done>Tailwind v4 compiled with full warm palette + semantic + typography tokens; Fontsource + Lovelace wired; index page uses token utilities; build produces CSS containing brand vars.</done>
</task>

<task type="auto">
  <name>Task 3.2: Init shadcn/ui with Tailwind v4 track and install Button component</name>
  <files>tsconfig.json, components.json, src/lib/utils.ts, src/components/ui/button.tsx, package.json, pnpm-lock.yaml</files>
  <read_first>
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/tsconfig.json (CURRENT — need to add @/* path alias before shadcn init)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-RESEARCH.md lines 196-197 (shadcn init commands)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-RESEARCH.md lines 396-400 (Pitfall 2 — shadcn init preconditions)
    - /Users/jashia/Documents/1_Projects/larraes-kitchen/.planning/phases/01-foundation/01-CONTEXT.md line 38 (D-08 — only Button installed)
    - https://ui.shadcn.com/docs/installation/astro (tsconfig paths + shadcn init flow for Astro)
  </read_first>
  <action>
1. **Add `@/*` path alias to `tsconfig.json`.** shadcn init requires this (Pitfall 2). Add under `compilerOptions`:
   ```json
   {
     "compilerOptions": {
       "baseUrl": ".",
       "paths": {
         "@/*": ["./src/*"]
       }
     }
   }
   ```
   Merge with whatever the Astro minimal template wrote. Confirm `strict: true` still present.

2. **Run shadcn init with defaults appropriate for Astro + Tailwind v4 + React.** shadcn CLI auto-detects Astro; pass defaults non-interactively where possible:
   ```bash
   pnpm dlx shadcn@latest init --base-color neutral --css src/styles/global.css --yes
   ```
   If the CLI prompts, accept:
   - Framework: astro (auto-detected)
   - Base color: neutral
   - CSS file: `src/styles/global.css`
   - React Server Components: no
   - Create a components.json: yes

   **If `--yes` is not supported by the installed shadcn major**, run `pnpm dlx shadcn@latest init` interactively and answer with the values above.

   This step creates `components.json` and `src/lib/utils.ts` (exports `cn()`). It MUST NOT remove or rewrite the `@theme` block in `src/styles/global.css` — shadcn v4-track init adds its own CSS vars but should leave custom `@theme` alone. **If shadcn overwrites `@theme`**, restore `src/styles/global.css` from git (`git diff src/styles/global.css` to inspect) and merge shadcn's additions manually under a separate `@layer base` block while keeping the full `@theme` intact.

3. **Install the Button component** (D-08 — only Button in Phase 1):
   ```bash
   pnpm dlx shadcn@latest add button --yes
   ```
   This writes `src/components/ui/button.tsx`.

4. **Verify imports resolve.** Create a temporary probe (delete after verify):
   ```bash
   cat > /tmp/_shadcn_probe.ts <<'EOF'
   import { Button } from "@/components/ui/button";
   import { cn } from "@/lib/utils";
   const _b = Button; const _c = cn;
   EOF
   # Use tsc via project's tsconfig to resolve — don't run the probe, just typecheck:
   pnpm exec astro check 2>&1 | tail -20
   rm -f /tmp/_shadcn_probe.ts
   ```
   `astro check` must exit 0 (there's already an index.astro; we just need zero errors).

5. **Build and confirm nothing regressed.**
   ```bash
   pnpm build
   ```
   Build succeeds; `dist/` or `.vercel/output/static/` still contains the `color-deep-amber` token (shadcn did not trash the `@theme` block).

**Do NOT:**
- Install any component other than Button in Phase 1 (D-08: Card, Accordion, Dialog, Input, Form, Label install in later phases on demand)
- Overwrite the `@theme` block in `src/styles/global.css` — if shadcn init tries to, preserve the brand tokens and merge shadcn's additions separately
- Run `shadcn init` BEFORE Tailwind v4 + React + the `@/*` path alias exist — that's Pitfall 2
  </action>
  <verify>
    <automated>cd /Users/jashia/Documents/1_Projects/larraes-kitchen && grep -q '"@/\*"' tsconfig.json && test -f components.json && test -f src/lib/utils.ts && grep -q 'export.*cn' src/lib/utils.ts && test -f src/components/ui/button.tsx && grep -qE 'export.*Button|export.*buttonVariants' src/components/ui/button.tsx && grep -q '@theme' src/styles/global.css && grep -q 'color-deep-amber' src/styles/global.css && pnpm exec astro check 2>&1 | tail -3 | grep -vi 'error' && pnpm build 2>&1 | tail -3 | grep -vi 'error' && (grep -rq 'color-deep-amber' dist/ 2>/dev/null || grep -rq 'color-deep-amber' .vercel/output/static/ 2>/dev/null)</automated>
  </verify>
  <acceptance_criteria>
    - `tsconfig.json` contains `"@/*": ["./src/*"]` in `compilerOptions.paths`
    - `components.json` exists (shadcn config)
    - `src/lib/utils.ts` exists and exports `cn` function
    - `src/components/ui/button.tsx` exists with named export `Button` (and typically `buttonVariants`)
    - `src/styles/global.css` STILL contains the full `@theme` block (shadcn init did NOT wipe brand tokens)
    - `src/styles/global.css` STILL contains `color-deep-amber` (sanity)
    - `pnpm exec astro check` exits 0 — `@/` path imports resolve
    - `pnpm build` exits 0 and built CSS still contains `color-deep-amber`
    - No component files other than `button.tsx` under `src/components/ui/` — per D-08 only Button is installed
  </acceptance_criteria>
  <done>shadcn/ui initialized with Tailwind v4 track; Button component installed and importable via `@/components/ui/button`; `cn()` utility available; design tokens preserved through shadcn init.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3.3: Checkpoint — visual + font verification</name>
  <files>— (human verification; no files modified)</files>
  <what-built>
    Design tokens compiled into CSS utilities, Fontsource fonts self-hosted, shadcn Button ready. Remaining verifications are inherently visual (font rendering, palette correctness) or depend on facts outside the repo (Lovelace license availability).
  </what-built>
  <action>Human visual check. Run `pnpm dev`, open http://localhost:4321/, and complete the steps in `<how-to-verify>`.</action>
  <how-to-verify>
    1. Run `pnpm dev` and open http://localhost:4321/ in a browser.
    2. Confirm the h1 "Larrae's Kitchen" renders in a serif face (Lovelace if font file supplied, otherwise Playfair Display fallback — both acceptable for Phase 1).
    3. Open DevTools → Network tab → reload. Confirm there are ZERO requests to `fonts.googleapis.com`, `fonts.gstatic.com`, or any other Google font CDN.
    4. Confirm the h1 color is the deep green brand color (`--color-greens-deep` = `#2E4A2F`) — dark sage/forest green, not black.
    5. Confirm background is warm cream (`--color-warm-cream` = `#F7EFD9`) — not plain white.
    6. If `public/fonts/Lovelace.woff2` is NOT yet supplied (expected in Phase 1), confirm the 404 for `/fonts/Lovelace.woff2` in DevTools — this is acceptable; Playfair fallback kicks in. Note whether the maintainer intends to supply the real file before Phase 5.

    **Decision point:** Is this a design/brand pass for what the scaffold can verify at this stage?
  </how-to-verify>
  <verify>Human approval via `<resume-signal>` after visual/network checks above.</verify>
  <done>Human confirms tokens render, brand colors correct, and no Google Fonts CDN requests are made.</done>
  <resume-signal>Type "approved" when the visual verification passes, or describe any issues. If the Lovelace font file is pending, note "Lovelace pending" and continue — that's a documented Phase 5 precondition, not a Phase 1 blocker.</resume-signal>
</task>

</tasks>

<verification>
Wave 3 gate:
- Tailwind v4 + `@theme` compiles (brand tokens appear in built CSS)
- shadcn init succeeded without wiping tokens
- Fonts load from Fontsource (self-hosted); no Google CDN requests
- `/` renders with brand green headline on warm cream background
</verification>

<success_criteria>
Tokens verified in built CSS bundle, shadcn Button importable, visual checkpoint approved by human.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` documenting:
- Tailwind version installed (`pnpm list tailwindcss @tailwindcss/vite`)
- Whether `Lovelace.woff2` was supplied or left pending
- Whether shadcn init disturbed `@theme` (and what merging was done if so)
- shadcn CLI version used (`pnpm dlx shadcn@latest --version`)
</output>
