---
phase: 02-content-static-sections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/hero/hero.md
  - src/content/about/about.md
autonomous: true
requirements: [CONT-08, CONT-09, HERO-01, HERO-02, HERO-03, HERO-04, ABOUT-01, ABOUT-02, ABOUT-03]
must_haves:
  truths:
    - "src/content/hero/hero.md passes heroSchema validation on astro sync"
    - "src/content/about/about.md passes aboutSchema validation (heritageNarrative ≥150 chars, ≤2500 chars)"
    - "Hero headline reads as a one-line soul-food value proposition referencing Benicia"
    - "Hero priceChip follows the 'From $X per person' pattern and $X matches the min per-person price across packages ($18 = Large tier floor)"
    - "About narrative is 150–250 words telling the heritage story; positioning line reads 'Benicia's only soul food specialist.'"
  artifacts:
    - path: "src/content/hero/hero.md"
      provides: "hero headline, subheadline, ctaText, priceChip, heroImage, heroImageAlt"
      contains: "headline:"
    - path: "src/content/about/about.md"
      provides: "heritageNarrative (150–250 words), positioning, chefPortrait placeholder, chefPortraitAlt"
      contains: "heritageNarrative:"
  key_links:
    - from: "src/content/hero/hero.md"
      to: "heroSchema"
      via: "Astro Content Collection loader"
      pattern: "heroImage:.*\\.jpg"
    - from: "src/content/about/about.md"
      to: "aboutSchema"
      via: "Astro Content Collection loader (validates heritageNarrative.min(150).max(2500))"
      pattern: "positioning:.*Benicia"
---

<objective>
Author `hero.md` and `about.md` — the two single-entry collections that seed the hero cinematic experience (HERO-01..04) and the About heritage narrative (ABOUT-01..03).

Purpose: Hero content is the site's first impression; About content delivers the "Benicia's only soul food specialist" positioning that conversion copy depends on.

Output: 2 markdown content files. No components yet.
</objective>

<execution_context>
@/Users/jashia/Documents/1_Projects/larraes-kitchen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jashia/Documents/1_Projects/larraes-kitchen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-static-sections/02-CONTEXT.md
@.planning/phases/02-content-static-sections/02-UI-SPEC.md
@.planning/phases/02-content-static-sections/02-PATTERNS.md
@src/lib/schemas/hero.ts
@src/lib/schemas/about.ts
@CLAUDE.md

<interfaces>
heroSchema — src/lib/schemas/hero.ts:
```typescript
export const heroSchema = z.object({
  headline: z.string(),
  subheadline: z.string().optional(),
  ctaText: z.string(),
  priceChip: z.string(),
  heroImage: z.string(),
  heroImageAlt: z.string(),
});
```

aboutSchema — src/lib/schemas/about.ts:
```typescript
export const aboutSchema = z.object({
  heritageNarrative: z.string().min(150).max(2500),
  positioning: z.string(),
  chefPortrait: z.string().optional(),
  chefPortraitAlt: z.string().optional(),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author hero.md</name>
  <files>src/content/hero/hero.md</files>
  <read_first>
    - src/lib/schemas/hero.ts (heroSchema shape)
    - .planning/phases/02-content-static-sections/02-PATTERNS.md § "src/content/hero/hero.md" — frontmatter pattern + anti-patterns
    - .planning/phases/02-content-static-sections/02-UI-SPEC.md § Copywriting Contract — CTA copy "Start your quote"; § Image Treatment Rules — hero image budget
    - .planning/phases/02-content-static-sections/02-CONTEXT.md D-01..D-04 — hero cinematic treatment
  </read_first>
  <action>
Create `src/content/hero/hero.md` with cinematic one-liner headline, soul-food subhead, exact CTA copy locked in UI-SPEC, and price chip reading "From $18 per person" (min per-person floor across tiers per PATTERNS.md § packages; plan 02-03 sets Large tier pricePerPerson.min = 18).

Exact frontmatter (use double quotes for every string; no smart quotes):

```markdown
---
headline: "Soul food that shows up."
subheadline: "Authentic catering for Benicia events — from backyard gatherings to boardroom lunches."
ctaText: "Start your quote"
priceChip: "From $18 per person"
heroImage: "/images/hero/hero.jpg"
heroImageAlt: "Golden cast-iron skillet of smothered chicken and rice on a linen-dressed table."
---
```

Notes:
- `heroImage` points to `/images/hero/hero.jpg` — actual file lives at `public/images/hero/hero.jpg`. Placeholder photography per STATE.md; AI agent swaps later. The `.md` file is correct even if the `.jpg` is not yet placed — schema does not check file existence.
- `ctaText: "Start your quote"` matches UI-SPEC § Copywriting Contract lock.
- `priceChip: "From $18 per person"` — $18 is the minimum pricePerPerson across packages (Large tier min).
- `heroImageAlt` describes the food scene, not the filename (A11Y-04).

Anti-patterns:
- Multiple `priority` images elsewhere (PERF-04 — hero is the only one; enforced in section components, not here).
- Hardcoding dollar amount that drifts from packages — $18 MUST match packages/large.md pricePerPerson.min in plan 02-03. If 02-03 diverges, this file needs updating.
  </action>
  <verify>
    <automated>pnpm astro sync 2>&1 | grep -qE "error" && exit 1 || pnpm astro sync 2>&1 | grep -qE "✓|synced" ; test -f src/content/hero/hero.md && grep -q 'ctaText: "Start your quote"' src/content/hero/hero.md && grep -q 'priceChip: "From \$18 per person"' src/content/hero/hero.md</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm astro sync` exits 0 with no hero-collection Zod errors
    - `grep -q 'headline: "Soul food that shows up."' src/content/hero/hero.md` matches
    - `grep -q 'ctaText: "Start your quote"' src/content/hero/hero.md` matches UI-SPEC lock
    - `grep -q 'priceChip: "From \$18 per person"' src/content/hero/hero.md` matches (pricing floor from PATTERNS § packages)
    - `heroImage` value starts with `/images/hero/` (relative path, Astro `<Image>` resolves at build)
    - `heroImageAlt` is non-empty and describes the scene (no filename-y values like "hero.jpg")
  </acceptance_criteria>
  <done>hero.md validates against heroSchema and carries all HERO-01..04 copywriting contract values.</done>
</task>

<task type="auto">
  <name>Task 2: Author about.md</name>
  <files>src/content/about/about.md</files>
  <read_first>
    - src/lib/schemas/about.ts (aboutSchema; heritageNarrative .min(150).max(2500))
    - .planning/phases/02-content-static-sections/02-PATTERNS.md § "src/content/about/about.md" — YAML block literal `|` pattern for multi-paragraph narrative
    - .planning/phases/02-content-static-sections/02-UI-SPEC.md § 3 AboutSection — narrative split on \n\n into `<p>` tags; positioning rendered as amber semibold subtitle
    - .planning/PROJECT.md — core value ("culturally rooted and effortlessly professional")
  </read_first>
  <action>
Create `src/content/about/about.md` with a 150–250 word heritage narrative (minimum 150 chars enforced by schema), ABOUT-03 positioning statement, and chef portrait placeholder paths.

Use YAML block literal `|` for `heritageNarrative` so paragraph breaks (blank lines) survive into the rendered component. PATTERNS.md § about confirms the component splits on `\n\n+` at render time.

```markdown
---
heritageNarrative: |
  Larrae grew up in her grandmother's kitchen in southern Louisiana, where Sunday suppers were a
  two-day affair — gumbo starting Friday afternoon, cornbread cooling on the counter, the whole
  house smelling of onion, bay leaf, and caramelized sugar. Recipes weren't written down; they
  were passed across a countertop, measured by hand, tasted for truth.

  She carried those recipes with her when she moved to Benicia, and she's been cooking them for
  her neighbors ever since. Larrae's Kitchen isn't a trend — it's soul food done the way it was
  taught: slow, generous, and exactly the way her grandmother would have served it. Every plate
  that leaves her kitchen is a direct line to the hands that taught her how to cook.

  She caters small family gatherings, backyard celebrations, and corporate lunches across the
  greater East Bay. If you're feeding people you love, she'd be honored to help.
positioning: "Benicia's only soul food specialist."
chefPortrait: "/images/about/chef-portrait.jpg"
chefPortraitAlt: "Chef Larrae standing at her kitchen counter with a smile, a cast-iron skillet beside her."
---
```

Notes:
- Narrative is three paragraphs, ~200 words, well inside 150–2500 char bounds.
- Paragraphs separated by blank lines (YAML `|` preserves newlines; component splits on `\n\n+`).
- `positioning` is the exact ABOUT-03 copy — rendered in amber semibold between H2 and narrative per UI-SPEC § 3.
- `chefPortrait` + `chefPortraitAlt` both provided so the AboutSection does not render the empty-state placeholder.

Anti-patterns:
- Narrative below 150 chars (schema fails, CI blocks).
- Moving narrative to markdown body (schema requires it in frontmatter string).
- Using curly quotes (— is acceptable; " should remain plain ASCII).
  </action>
  <verify>
    <automated>pnpm astro sync 2>&1 | grep -qE "error" && exit 1 || pnpm astro sync 2>&1 | grep -qE "✓|synced" ; test -f src/content/about/about.md && grep -q "positioning:" src/content/about/about.md && grep -q "heritageNarrative: |" src/content/about/about.md</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm astro sync` exits 0 with no about-collection Zod errors
    - `grep -q "heritageNarrative: |" src/content/about/about.md` matches (YAML block literal form)
    - `grep -q "Benicia" src/content/about/about.md` matches (positioning line references Benicia)
    - `grep -q "specialist" src/content/about/about.md` matches (positioning copy)
    - Narrative content length ≥ 150 chars: `awk "/^heritageNarrative: \\|/{f=1;next} /^[a-z]/{f=0} f" src/content/about/about.md | wc -c` outputs a number ≥ 150
    - `grep -q "chefPortrait:" src/content/about/about.md` matches (placeholder path present)
    - `grep -q "chefPortraitAlt:" src/content/about/about.md` matches (alt text present if path present — UI-SPEC § Alt Text Rules)
  </acceptance_criteria>
  <done>about.md validates against aboutSchema, narrative is authored with heritage voice, positioning line locked, placeholder chef portrait declared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown frontmatter → Astro Content Collection | Author input; Zod validates at build time (FND-05 CI gate) |
| rendered narrative text → browser HTML | Phase 2 renders as plain text (not `set:html`) — no XSS surface from content for now |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | hero.md priceChip drifts from packages pricePerPerson.min | mitigate | PATTERNS.md § packages locks $18 floor; Phase 4 can add a build-time assertion (Node script) cross-checking the string. For Phase 2, rely on author discipline + code review. |
| T-02-05 | Information Disclosure | about.md narrative | accept | Public marketing copy by design; no PII. |
| T-02-06 | Tampering | about.md heritageNarrative under 150 chars bypassing schema | mitigate | Zod `.min(150)` enforced by astro sync in CI (FND-05). Build fails before deploy. |
</threat_model>

<verification>
1. `pnpm astro sync` succeeds — both hero and about collections validated.
2. `grep` assertions confirm locked copywriting strings match UI-SPEC contract.
3. Narrative character count ≥ 150 (schema floor).
</verification>

<success_criteria>
- hero.md carries HERO-01..04 data (image path, headline, subheadline, CTA text, price chip) with schema-valid fields.
- about.md carries ABOUT-01..03 data (narrative ≥150 chars, positioning, chef portrait placeholder) with schema-valid fields.
- Both files pass `astro sync` (FND-05 CI gate).
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-static-sections/02-02-SUMMARY.md`.
</output>
