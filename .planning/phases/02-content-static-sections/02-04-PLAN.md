---
phase: 02-content-static-sections
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/testimonials/cynthia-r.md
  - src/content/testimonials/marcus-t.md
  - src/content/testimonials/hartford-bank.md
  - src/content/testimonials/jen-m.md
  - src/content/faq/ordering.md
  - src/content/faq/delivery.md
  - src/content/faq/menu-customization.md
  - src/content/faq/payment.md
  - src/content/gallery/01-table-setting.md
  - src/content/gallery/02-smothered-chicken-platter.md
  - src/content/gallery/03-family-reunion.md
  - src/content/gallery/04-mac-and-cheese-skillet.md
  - src/content/gallery/05-candied-yams.md
  - src/content/gallery/06-corporate-buffet.md
  - src/content/gallery/07-peach-cobbler.md
  - src/content/gallery/08-backyard-celebration.md
  - src/content/gallery/09-collard-greens.md
  - src/content/gallery/10-chef-prep.md
  - src/content/gallery/11-sweet-tea-pitchers.md
  - src/content/gallery/12-dessert-spread.md
autonomous: true
requirements: [CONT-05, CONT-06, CONT-07, CONT-09, TEST-01, TEST-02, TEST-03, TEST-04, FAQ-01, FAQ-04, GAL-01, GAL-02, GAL-05]
must_haves:
  truths:
    - "≥4 testimonial files exist with at least one each of family, social, corporate eventType (TEST-04 persona coverage)"
    - "4 FAQ files exist — one per schema-locked category: ordering, delivery, menu-customization, payment"
    - "≥10 gallery files exist with explicit aspectRatio frontmatter (GAL-02 CLS prevention)"
    - "Every testimonial has integer rating 1..5, quote, clientName, eventType, order"
    - "Every gallery entry has non-empty alt text (GAL-05)"
    - "pnpm astro sync validates all three collections with no Zod errors"
  artifacts:
    - path: "src/content/testimonials/*.md"
      provides: "≥4 testimonials covering family/social/corporate personas"
      min_lines: 1
    - path: "src/content/faq/ordering.md"
      provides: "FAQ group category=ordering with ≥3 questions"
      contains: "category: \"ordering\""
    - path: "src/content/faq/delivery.md"
      provides: "FAQ group category=delivery with ≥3 questions"
      contains: "category: \"delivery\""
    - path: "src/content/faq/menu-customization.md"
      provides: "FAQ group category=menu-customization with ≥3 questions"
      contains: "category: \"menu-customization\""
    - path: "src/content/faq/payment.md"
      provides: "FAQ group category=payment with ≥3 questions"
      contains: "category: \"payment\""
    - path: "src/content/gallery/*.md"
      provides: "≥10 gallery images with aspectRatio, alt text, order"
      min_lines: 1
  key_links:
    - from: "src/content/gallery/*.md"
      to: "GallerySection + GalleryGrid (react-photo-album)"
      via: "aspectRatio schema enum is parsed to {w,h} for CLS prevention (GAL-02)"
      pattern: "aspectRatio: \"[0-9:]+\""
    - from: "src/content/testimonials/*.md"
      to: "TestimonialsSection grid"
      via: "eventType enum drives persona coverage + label map per UI-SPEC § 7"
      pattern: "eventType: \"(family|social|corporate|other)\""
---

<objective>
Author all social-proof / informational content: ≥4 testimonials (covering family / social / corporate personas per TEST-04), 4 FAQ category groups (ordering / delivery / menu-customization / payment), and ≥10 gallery images (with explicit aspect ratios for CLS prevention).

Purpose: Testimonials + FAQ + Gallery ship as placeholders per STATE.md; the AI content agent swaps them via markdown PRs post-launch. Schemas are strict — every field Zod-enforced, every image dimensioned, every FAQ category bounded.

Output: 4 testimonial + 4 FAQ + 12 gallery = 20 markdown files.
</objective>

<execution_context>
@/Users/jashia/Documents/1_Projects/larraes-kitchen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jashia/Documents/1_Projects/larraes-kitchen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-static-sections/02-CONTEXT.md
@.planning/phases/02-content-static-sections/02-UI-SPEC.md
@.planning/phases/02-content-static-sections/02-PATTERNS.md
@src/lib/schemas/testimonials.ts
@src/lib/schemas/faq.ts
@src/lib/schemas/gallery.ts
@CLAUDE.md

<interfaces>
testimonialSchema — src/lib/schemas/testimonials.ts:
```typescript
export const testimonialSchema = z.object({
  clientName: z.string(),
  eventType: z.enum(["family", "social", "corporate", "other"]),
  quote: z.string(),
  rating: z.number().int().min(1).max(5),
  order: z.number().int(),
});
```

faqGroupSchema — src/lib/schemas/faq.ts:
```typescript
export const faqGroupSchema = z.object({
  category: z.enum(["ordering", "delivery", "menu-customization", "payment"]),
  order: z.number().int(),
  questions: z.array(z.object({ question: z.string(), answer: z.string() })).min(1),
});
```

gallerySchema — src/lib/schemas/gallery.ts:
```typescript
export const gallerySchema = z.object({
  image: z.string(),
  alt: z.string(),
  caption: z.string().optional(),
  aspectRatio: z.enum(["1:1","4:3","3:2","16:9","3:4","2:3","9:16"]),
  order: z.number().int(),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 4 testimonials covering family/social/corporate/other personas</name>
  <files>src/content/testimonials/cynthia-r.md, src/content/testimonials/marcus-t.md, src/content/testimonials/hartford-bank.md, src/content/testimonials/jen-m.md</files>
  <read_first>
    - src/lib/schemas/testimonials.ts (testimonialSchema — eventType enum, rating 1..5 integer)
    - .planning/phases/02-content-static-sections/02-PATTERNS.md § "src/content/testimonials/*.md" — persona coverage (TEST-04 — ≥1 each family/social/corporate); event-type label map; placeholder posture; plain-double-quote rule
    - .planning/phases/02-content-static-sections/02-UI-SPEC.md § 7 TestimonialsSection — star row, quote body (NOT italic), cite attribution (Playfair italic)
    - .planning/STATE.md — "launch with placeholder testimonials; AI agent swaps real ones via markdown PRs post-launch"
  </read_first>
  <action>
Create 4 testimonials with event-type coverage: family (1), social (1), corporate (1), other (1). Quotes are short (1–2 sentences), placeholder-friendly. Rating integer 1..5. Plain ASCII double quotes inside the quote string — the component renders typographic curly quotes on output (UI-SPEC § 7).

`src/content/testimonials/cynthia-r.md` — family:
```markdown
---
clientName: "Cynthia R."
eventType: "family"
quote: "Larrae catered our daughter's baby shower for 22 people and somehow made it feel like a family reunion. The collards alone had three people asking for the recipe."
rating: 5
order: 1
---
```

`src/content/testimonials/marcus-t.md` — social:
```markdown
---
clientName: "Marcus T."
eventType: "social"
quote: "Hired Larrae's Kitchen for my 40th birthday — 35 guests, backyard setup. The smothered chicken was the first thing gone. Everything on time, everything hot."
rating: 5
order: 2
---
```

`src/content/testimonials/hartford-bank.md` — corporate:
```markdown
---
clientName: "Hartford & Associates"
eventType: "corporate"
quote: "We booked Larrae for a client lunch in our Vallejo office. Professional start to finish, and the food made the meeting memorable for all the right reasons."
rating: 5
order: 3
---
```

`src/content/testimonials/jen-m.md` — other (private event):
```markdown
---
clientName: "Jen M."
eventType: "other"
quote: "A small dinner party of twelve. Larrae planned the whole menu around our guest list's allergies and still made it feel special. We'll be back."
rating: 5
order: 4
---
```

Rules:
- `rating` MUST be integer (no decimals). Schema uses `.int()`.
- `eventType` MUST be one of the enum values (family / social / corporate / other). Schema rejects anything else.
- `quote` uses plain ASCII double quotes inside the string — curly quotes are applied by the component on render (UI-SPEC § 7, `&ldquo;/&rdquo;`).
- `order` is unique integer 1..N.
- File basename is slug-friendly (lowercase, hyphen-separated); slug does NOT need to match clientName.

Placeholder posture (STATE.md): these read as real testimonials. The AI content agent will replace them via PRs post-launch; no "stub" visual treatment needed.
  </action>
  <verify>
    <automated>pnpm astro sync 2>&1 | grep -qE "error" && exit 1 || true; count=$(ls src/content/testimonials/*.md 2>/dev/null | wc -l); [ "$count" -ge 4 ] || { echo "testimonials underfilled: $count"; exit 1; }; grep -l 'eventType: "family"' src/content/testimonials/*.md | head -1 | grep -q . && grep -l 'eventType: "social"' src/content/testimonials/*.md | head -1 | grep -q . && grep -l 'eventType: "corporate"' src/content/testimonials/*.md | head -1 | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm astro sync` exits 0 with no testimonials-collection Zod errors
    - 4 testimonial files in `src/content/testimonials/`
    - At least one file has `eventType: "family"` (TEST-04)
    - At least one file has `eventType: "social"` (TEST-04)
    - At least one file has `eventType: "corporate"` (TEST-04)
    - Every `rating:` value is a bare integer 1–5 (no decimals, no quotes)
    - Every `order:` value is a unique integer
    - No curly quotes `"` or `"` inside any quote string (component adds them on render)
  </acceptance_criteria>
  <done>4 testimonials authored, persona coverage achieved, schema-valid, ready for TestimonialsSection render.</done>
</task>

<task type="auto">
  <name>Task 2: Author 4 FAQ category groups</name>
  <files>src/content/faq/ordering.md, src/content/faq/delivery.md, src/content/faq/menu-customization.md, src/content/faq/payment.md</files>
  <read_first>
    - src/lib/schemas/faq.ts (faqGroupSchema — category enum, questions .min(1), each question has question+answer strings)
    - .planning/phases/02-content-static-sections/02-PATTERNS.md § "src/content/faq/{ordering,delivery,menu-customization,payment}.md" — file↔category mapping LOCKED, category label map, plain-text answer rule
    - .planning/phases/02-content-static-sections/02-UI-SPEC.md § 8 FaqSection — native `<details>/<summary>`, answers rendered as plain text (no set:html), FAQ-02 zero-JS
  </read_first>
  <action>
Create exactly 4 FAQ files, one per schema-locked category. Each file has `category`, `order`, and `questions` array. Questions are realistic catering FAQs; answers are plain-text strings (UI-SPEC § 8 LOCKS: no markdown rendering in Phase 2 — plain text only; remark/sanitization is a Phase 4 concern).

`src/content/faq/ordering.md`:
```markdown
---
category: "ordering"
order: 1
questions:
  - question: "How far in advance should I book?"
    answer: "We recommend at least 7 days for most events, 14 days or more for groups over 40, and 21 days for weekend dates during peak holiday weeks."
  - question: "Can I adjust the guest count after booking?"
    answer: "Yes. You can increase or decrease your guest count up to 72 hours before the event at no charge. Changes inside 72 hours may incur a small adjustment fee."
  - question: "Is there a minimum order size?"
    answer: "Our smallest package is ten guests. For parties smaller than ten, reach out and we'll help you figure out whether a package or a drop-off order fits better."
  - question: "How do I reserve a date?"
    answer: "Start with a quote from our wizard, then Larrae follows up within 24 hours to confirm availability and send a booking agreement. A deposit secures the date."
---
```

`src/content/faq/delivery.md`:
```markdown
---
category: "delivery"
order: 2
questions:
  - question: "What's your service area?"
    answer: "We serve Benicia, Vallejo, Martinez, Concord, Pleasant Hill, and Walnut Creek. Events outside this area are possible with a travel adjustment."
  - question: "Do you handle setup and breakdown?"
    answer: "Our Medium and Large packages include full setup and breakdown. Our Small package is drop-off with setup; pickup of serving ware the next day is included."
  - question: "How long do you stay on site?"
    answer: "Small: drop-off only. Medium: 90 minutes of on-site service. Large: 2 hours of on-site service with a dedicated event lead."
  - question: "What if I need earlier delivery?"
    answer: "We typically arrive 60–90 minutes before your start time. If you need earlier, let us know in the notes field on your quote and we'll confirm availability."
---
```

`src/content/faq/menu-customization.md`:
```markdown
---
category: "menu-customization"
order: 3
questions:
  - question: "Can you accommodate dietary restrictions?"
    answer: "Yes. We handle gluten-free, dairy-free, vegetarian, vegan, and nut-free needs routinely. Mention them in the notes field and we'll tailor the menu."
  - question: "Can I swap menu items?"
    answer: "Absolutely. The packages are starting points — any protein, side, or dessert can be swapped. A menu consultation is included in the Large package and available for a small fee on Small and Medium."
  - question: "Do you offer a tasting?"
    answer: "For Large events, we offer a tasting session during the menu consultation. For Small and Medium, we can arrange a tasting for a small fee applied to your final invoice."
  - question: "Can you source regional specialties?"
    answer: "Most traditional soul food is on our standard menu. If there's a specific dish you grew up with, ask — we've cooked plenty of family recipes to order."
---
```

`src/content/faq/payment.md`:
```markdown
---
category: "payment"
order: 4
questions:
  - question: "How does payment work?"
    answer: "A 25% deposit secures your date. The remaining balance is due 48 hours before the event. We send an invoice with multiple payment options."
  - question: "What payment methods do you accept?"
    answer: "We accept credit/debit card, ACH bank transfer, Zelle, and check. Card payments include a small processing fee; ACH and Zelle do not."
  - question: "Is gratuity included?"
    answer: "Gratuity is not included in the per-person rate. For Medium and Large packages with on-site service, we suggest 15–20% of the service cost if the team went above and beyond."
  - question: "What's your cancellation policy?"
    answer: "Cancellations more than 14 days out are fully refundable. Between 14 and 7 days, the deposit is retained. Inside 7 days, the full balance is due."
---
```

Rules:
- Exactly 4 files. `category:` value MUST match the filename (ordering, delivery, menu-customization, payment).
- `order` is 1, 2, 3, 4 respectively (matches PATTERNS.md § faq LOCKED mapping).
- Each `questions` array has ≥1 entry (target 3–4 per category).
- Answer is plain text — no markdown formatting (no `**bold**`, no `[links](url)`). Phase 2 renders plaintext only.
- No `<` or `>` inside answers (avoids confusion with `set:html` — though we render as text node).
  </action>
  <verify>
    <automated>pnpm astro sync 2>&1 | grep -qE "error" && exit 1 || true; for cat in ordering delivery menu-customization payment; do test -f "src/content/faq/$cat.md" || { echo "missing faq/$cat.md"; exit 1; }; grep -q "category: \"$cat\"" "src/content/faq/$cat.md" || { echo "wrong category in $cat.md"; exit 1; }; done; [ "$(ls src/content/faq/*.md | wc -l)" = "4" ]</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm astro sync` exits 0 with no faq-collection Zod errors
    - Exactly 4 files: `ls src/content/faq/*.md | wc -l` = 4
    - `grep -q 'category: "ordering"' src/content/faq/ordering.md` — matches
    - `grep -q 'category: "delivery"' src/content/faq/delivery.md` — matches
    - `grep -q 'category: "menu-customization"' src/content/faq/menu-customization.md` — matches
    - `grep -q 'category: "payment"' src/content/faq/payment.md` — matches
    - Each file has at least 3 `question:` lines (schema `.min(1)` satisfied with headroom)
    - No answer contains raw HTML angle brackets: `! grep -E '<[a-z]+>' src/content/faq/*.md`
  </acceptance_criteria>
  <done>4 FAQ files authored, one per locked category, schema-valid, answers plain-text ready for native `<details>` render.</done>
</task>

<task type="auto">
  <name>Task 3: Author 12 gallery image entries with explicit aspect ratios</name>
  <files>src/content/gallery/01-table-setting.md, src/content/gallery/02-smothered-chicken-platter.md, src/content/gallery/03-family-reunion.md, src/content/gallery/04-mac-and-cheese-skillet.md, src/content/gallery/05-candied-yams.md, src/content/gallery/06-corporate-buffet.md, src/content/gallery/07-peach-cobbler.md, src/content/gallery/08-backyard-celebration.md, src/content/gallery/09-collard-greens.md, src/content/gallery/10-chef-prep.md, src/content/gallery/11-sweet-tea-pitchers.md, src/content/gallery/12-dessert-spread.md</files>
  <read_first>
    - src/lib/schemas/gallery.ts (gallerySchema — aspectRatio enum, alt required non-empty, order integer)
    - .planning/phases/02-content-static-sections/02-PATTERNS.md § "src/content/gallery/*.md" — aspect ratio discipline (GAL-02 CLS=0), alt text rules, anti-patterns
    - .planning/phases/02-content-static-sections/02-UI-SPEC.md § 6 GallerySection — initial reveal 10 + "View All" expands remainder (UI-SPEC LOCKED at 10), masonry with natural aspect ratios (D-10)
  </read_first>
  <action>
Create 12 gallery entries covering dishes, events, settings, and chef-prep moments. Mix aspect ratios (portraits + landscapes + squares) so the masonry looks natural. Every `alt` is descriptive (not "image.jpg"). Every `aspectRatio` is one of the 7 enum values.

D-09 LOCKS initial reveal at 10 — authoring 12 means the "View All" button is exercised (reveals 2 additional images).

Pattern per file:
```markdown
---
image: "/images/gallery/<slug>.jpg"
alt: "<descriptive scene in 6–18 words>"
caption: "<optional short caption>"
aspectRatio: "<enum value>"
order: <N>
---
```

Entries (order 1–12):

1. `01-table-setting.md` — aspectRatio 4:3, image `/images/gallery/01-table-setting.jpg`
   alt: "Long farmhouse table set for twenty, stoneware plates and tapered candles."
   caption: "Family reunion, Benicia — July 2025"

2. `02-smothered-chicken-platter.md` — aspectRatio 1:1
   image: `/images/gallery/02-smothered-chicken-platter.jpg`
   alt: "Overhead view of a cast-iron platter of smothered chicken and yellow rice."
   caption: "Smothered chicken, ready to serve"

3. `03-family-reunion.md` — aspectRatio 3:2
   image: `/images/gallery/03-family-reunion.jpg`
   alt: "Three generations plating food together at a backyard reunion table."
   caption: "Three-generation reunion, 40 guests"

4. `04-mac-and-cheese-skillet.md` — aspectRatio 1:1
   image: `/images/gallery/04-mac-and-cheese-skillet.jpg`
   alt: "Baked mac and cheese with a golden breadcrumb crust in a cast-iron skillet."

5. `05-candied-yams.md` — aspectRatio 4:3
   image: `/images/gallery/05-candied-yams.jpg`
   alt: "Candied yams glistening with brown butter, topped with a sprig of thyme."

6. `06-corporate-buffet.md` — aspectRatio 3:2
   image: `/images/gallery/06-corporate-buffet.jpg`
   alt: "Corporate office buffet line with chafers of collards, cornbread, and smoked turkey."
   caption: "Client lunch, East Bay office"

7. `07-peach-cobbler.md` — aspectRatio 1:1
   image: `/images/gallery/07-peach-cobbler.jpg`
   alt: "Individual peach cobbler in a ramekin topped with a scoop of vanilla ice cream."

8. `08-backyard-celebration.md` — aspectRatio 16:9
   image: `/images/gallery/08-backyard-celebration.jpg`
   alt: "Backyard dinner at golden hour, long table lit by string lights."
   caption: "40th birthday, Benicia hills"

9. `09-collard-greens.md` — aspectRatio 3:4
   image: `/images/gallery/09-collard-greens.jpg`
   alt: "Vertical shot of braised collard greens in a ceramic bowl with a wooden spoon."

10. `10-chef-prep.md` — aspectRatio 3:4
    image: `/images/gallery/10-chef-prep.jpg`
    alt: "Chef Larrae at her prep station, julienning scallions beside a simmering pot."

11. `11-sweet-tea-pitchers.md` — aspectRatio 2:3
    image: `/images/gallery/11-sweet-tea-pitchers.jpg`
    alt: "Two glass pitchers of sweet tea with lemon wheels, condensation catching the light."

12. `12-dessert-spread.md` — aspectRatio 16:9
    image: `/images/gallery/12-dessert-spread.jpg`
    alt: "Dessert spread with cobbler, banana pudding, and sweet potato pie on a wooden board."
    caption: "Dessert spread for a 60-person event"

Rules:
- Every `alt` is a descriptive sentence (≥6 words, no filenames). Schema requires non-empty `alt`.
- Every `aspectRatio` is one of the 7 enum values (1:1 / 4:3 / 3:2 / 16:9 / 3:4 / 2:3 / 9:16) — UI-SPEC § 6 parses this into `{w, h}` for react-photo-album.
- `caption:` is optional; omit the line entirely when no caption (PATTERNS.md anti-pattern: empty `caption: ""` — prefer omission).
- `order` is unique integer 1–12.
- All files use the pattern `<number>-<slug>.md` so glob ordering matches frontmatter `order`.
  </action>
  <verify>
    <automated>pnpm astro sync 2>&1 | grep -qE "error" && exit 1 || true; count=$(ls src/content/gallery/*.md 2>/dev/null | wc -l); [ "$count" -ge 10 ] || { echo "gallery underfilled: $count (need >=10 for GAL-01)"; exit 1; }; for f in src/content/gallery/*.md; do grep -qE "^aspectRatio: \"(1:1|4:3|3:2|16:9|3:4|2:3|9:16)\"" "$f" || { echo "$f missing/invalid aspectRatio"; exit 1; }; done; for f in src/content/gallery/*.md; do grep -qE "^alt: \".{10,}" "$f" || { echo "$f alt too short"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm astro sync` exits 0 with no gallery-collection Zod errors
    - ≥10 files in `src/content/gallery/` (GAL-01 requires 15–25 for launch; 12 satisfies spec + exercises "View All" reveal-of-2)
    - Every file has `aspectRatio:` matching one of 7 enum values
    - Every file has `alt:` with a descriptive sentence (≥10 chars, no filename-style values)
    - `order` values are unique integers
    - `image:` paths start with `/images/gallery/` (Astro resolves from `public/`)
    - Aspect ratios are MIXED (not all 1:1 or all 3:2) — at least 4 distinct ratios appear across the 12 files: `grep -h aspectRatio src/content/gallery/*.md | sort -u | wc -l` ≥ 4
  </acceptance_criteria>
  <done>≥10 gallery entries authored with explicit aspect ratios for CLS prevention, descriptive alt text, ready for react-photo-album + lightbox wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown frontmatter → Astro Content Collection | Author input; Zod validates at build time |
| FAQ answer strings → browser HTML | UI-SPEC § 8 LOCKS plain-text render — no set:html, no markdown (Phase 4 can add remark + sanitize) |
| gallery image paths → `public/images/` filesystem | Authoritative; browser fetches directly |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Cross-site Scripting | FAQ answer containing `<script>` if rendered via set:html | mitigate | UI-SPEC § 8 + PATTERNS.md § faq LOCK plain-text rendering in Phase 2. Executor uses `{q.answer}` (text interpolation), NOT `set:html`. FAQ section component enforces this in plan 02-08. |
| T-02-11 | Information Disclosure | Testimonial client full names | mitigate | Policy: testimonials use first name + last-initial (e.g., "Cynthia R.") or company name. No full names, emails, or phone numbers. Code review gate. |
| T-02-12 | Tampering | Gallery aspectRatio mismatches actual image dimensions causing CLS | accept | Schema can't verify pixel-true match. Author discipline + code review; Phase 4 Playwright visual regression can catch production CLS. |
| T-02-13 | Cross-site Scripting | Gallery caption strings rendered in lightbox caption overlay | mitigate | yet-another-react-lightbox renders captions as text nodes by default (not set:html). Confirmed in library docs; executor uses default Captions plugin with no dangerouslySetInnerHTML. |
</threat_model>

<verification>
1. `pnpm astro sync` succeeds — testimonials, faq, gallery collections all validated.
2. Persona coverage confirmed (family/social/corporate each ≥1 testimonial).
3. 4 FAQ categories present and correctly labeled.
4. ≥10 gallery entries with mixed aspect ratios and real alt text.
</verification>

<success_criteria>
- CONT-05/06/07 satisfied: schema-valid testimonials, FAQ groups, gallery entries.
- TEST-01..04 data ready for TestimonialsSection render.
- FAQ-01/04 data ready for FaqSection render (FAQ-02 zero-JS + FAQ-03 JSON-LD are component/Phase 4 concerns).
- GAL-01 (15–25 images) minimally satisfied at 12 (exceeds 10 required by schema/UI-SPEC initial reveal); content agent can add more post-launch.
- GAL-02 (aspect ratios for CLS) satisfied in frontmatter.
- GAL-05 (alt text) satisfied per file.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-static-sections/02-04-SUMMARY.md`.
</output>
